# 语义感知动态避障论文思路

## 一句话主线

本文提出一种面向移动机器人的**语义感知动态安全规划框架**：用视觉语义识别障碍物类别，用激光/里程计估计障碍物动态状态和预测轨迹，再根据类别与交互上下文生成语义安全余量 `β_i`，通过 Safety Guard 保证余量更新不会破坏安全可行性，最后将 `β_i` 与障碍物预测轨迹共同作用于全局风险搜索和局部 MPC-SECBF 轨迹优化。

更短的论文摘要式表达：

> 用语义感知回答“这个障碍物是谁”，用动态感知回答“它在哪里、怎么动、未来往哪里走”，再将二者统一成语义动态障碍物表征，生成受 Guard 保护的语义安全余量，并嵌入 MPC-SECBF 动态避障规划。

---

# 一、问题定义

传统动态避障方法通常只建模障碍物的几何和运动状态：

$$
o_i = \{p_i^o,\ v_i^o,\ R_i\}
$$

其中 `p_i^o` 是障碍物位置，`v_i^o` 是障碍物速度，`R_i` 是障碍物等效半径。这样的模型能避免碰撞，但它只告诉机器人：

> 前面有个障碍物，要绕开。

它不知道这个障碍物是纸箱、成人、儿童、骑车人还是车辆。因此，传统方法往往使用统一固定安全距离。以 Zhang2026 一类 baseline 为例，可写成：

$$
h_{\mathrm{Zhang}} =
\|l+\tau v\|_2 - R_{\mathrm{obs}} - R_{\mathrm{robot}} - d_{\mathrm{safe}}
$$

其中 `d_safe` 是人为设定的固定安全余量，例如 `0.4 m`。

这个做法的问题是：

1. 对所有障碍物采用同一安全余量，不够语义敏感。
2. 纸箱、成人、儿童、骑车人等对象应该对应不同避让尺度。
3. 固定大安全距离会让 MPC 更保守，甚至更容易不可行。
4. 固定小安全距离又会降低社会合规性和交互舒适度。

因此，本文将固定安全余量替换为可解释、可动态调节、且经过可行性审查的语义安全余量 `β_i`。

---

# 二、核心公式体系

## 1. 基础动态安全函数

首先定义不含语义余量的基础安全函数：

$$
h_{EE}(i,k)
=
\|p^{pred}_{obs,i,k} - p_{robot,k}\|_2
- R_{obs,i}
- R_{robot}
$$

其中：

- `p^{pred}_{obs,i,k}` 是第 `i` 个障碍物在第 `k` 个预测步的预测位置。
- `p_{robot,k}` 是机器人在第 `k` 个预测步的位置。
- `R_obs,i` 是障碍物半径。
- `R_robot` 是机器人半径。

如果显式写出相对速度前瞻形式，则：

$$
h_{EE}(i,k)
=
\|p_{rel,i,k}+\tau v_{rel,i,k}\|_2
- R_{obs,i}
- R_{robot}
$$

这里：

$$
p_{rel,i,k}=p_{obs,i,k}-p_{robot,k}
$$

$$
v_{rel,i,k}=v_{obs,i,k}-v_{robot,k}
$$

在当前代码里，MPC 端的障碍物位置已经由 `globalFsm/obs_manager` 按预测步外推，因此 `obs(0), obs(1)` 已经隐含了未来位置。Guard 端则显式使用：

```cpp
lookahead_rel_pos = p_rel + tau * v_rel;
```

来计算 `h_EE`。

---

## 2. 语义增强安全函数

本文不再额外加入固定 `d_safe`，而是引入语义安全余量：

$$
\beta_{i,k}
$$

语义增强安全函数定义为：

$$
h_{SEE}(i,k)
=
h_{EE}(i,k)-\beta_{i,k}
$$

展开后：

$$
h_{SEE}(i,k)
=
\|p_{rel,i,k}+\tau v_{rel,i,k}\|_2
- R_{obs,i}
- R_{robot}
- \beta_{i,k}
$$

这就是论文和代码应统一使用的核心形式。

需要特别避免的写法是把“机器人半径”和“固定安全距离”混成一个额外安全半径：

$$
R_{obs}+R_{extra}+\beta_i
$$

这种写法会让 `R_extra` 的物理含义不清。本文采用更清晰的写法：

$$
R_{obs}+R_{robot}+\beta_i
$$

也就是说：

- `R_robot` 是机器人物理半径。
- `β_i` 是语义安全余量。
- 不再额外叠加固定 `d_safe`。

---

## 3. 语义安全余量生成

候选语义安全余量由类别和上下文共同决定：

$$
\hat{\beta}_{i,k}
=
\bar{\beta}(c_i)\mu(\phi_{i,k})
$$

其中：

- `c_i` 是障碍物类别，例如 pedestrian、child、cyclist、vehicle、box、unknown。
- `\bar{\beta}(c_i)` 是类别基础安全余量。
- `\phi_{i,k}` 是交互上下文特征。
- `\mu(\phi_{i,k})` 是上下文调制系数。

当前代码中的上下文调制形式可以写成：

$$
\mu(\phi)
=
\mathrm{clip}
\left(
w_0
+ w_h f_{heading}
+ w_t f_{ttc}
+ w_d f_{density},
0,\ 1
\right)
$$

因此：

$$
\hat{\beta}_{i,k}
=
\bar{\beta}(c_i)
\cdot
\mathrm{clip}
\left(
w_0
+ w_h f_{heading}
+ w_t f_{ttc}
+ w_d f_{density},
0,\ 1
\right)
$$

直观解释：

- 儿童、骑车人、车辆等类别可以有不同 `β_bar`。
- 正在靠近的障碍物可以增大 `β`。
- TTC 越小，语义余量越大。
- 障碍物密度越高，语义余量越大。

---

## 4. Safety Guard

候选语义余量 `β_hat` 不能无条件进入 MPC，因为过大的语义余量可能让约束过紧，导致优化不可行。

因此引入 Guard：

$$
\hat{\beta}_{i,k}
\leq
h_{EE}(i,k)-\eta
$$

等价地：

$$
h_{SEE}(i,k)
=
h_{EE}(i,k)-\beta_{i,k}
\geq
\eta
$$

如果使用软安全解释，也可以表述为：

$$
h_{SEE}(i,k) \geq -\eta
$$

当前代码里的 Guard 判断是：

```cpp
guard_pass = (beta_hat <= h_ee - eta);
```

当 Guard 通过时，对 `β_hat` 做速率限制：

$$
\beta_{i,k}
=
\beta_{i,k-1}
+
\mathrm{clip}
(\hat{\beta}_{i,k}-\beta_{i,k-1},
-\Delta \bar{\beta},
\Delta \bar{\beta})
$$

当 Guard 不通过时，回退到上一帧：

$$
\beta_{i,k}
=
\beta_{i,k-1}
$$

这样可以避免语义余量突然变大造成 MPC 不可行。

---

## 5. MPC-SECBF 约束

局部规划器使用 MPC-SECBF 约束：

$$
h_{SEE}(i,k+1)
\geq
(1-\gamma)h_{SEE}(i,k)
$$

等价写成优化器中的不等式：

$$
-h_{SEE}(i,k+1)
+
(1-\gamma)h_{SEE}(i,k)
\leq
0
$$

当前代码 `mpc_secbf.cpp` 中的约束形式正是：

```cpp
cbf = -hk1 + (1.0 - gamma_) * hk;
prob.subject_to(cbf <= 0);
```

---

# 三、与 Zhang2026 Baseline 的关系

Zhang2026 类方法可理解为固定安全余量：

$$
h_{\mathrm{Zhang}}
=
\|l+\tau v\|_2
- R_{obs}
- R_{robot}
- d_{safe}
$$

本文方法为：

$$
h_{SEE}
=
\|l+\tau v\|_2
- R_{obs}
- R_{robot}
- \beta_i
$$

二者的等价关系是：

$$
d_{safe}=0.4
\quad
\Longleftrightarrow
\quad
\beta_i \equiv 0.4
$$

也就是说，当所有障碍物类别都为 `unknown`，且语义余量恒定为 `0.4 m` 时，本文方法退化为 Zhang2026 的固定安全距离形式。

| 项目 | Zhang2026 baseline | 本文 MPC-SECBF |
|---|---|---|
| 安全函数 | `||l+τv|| - R_obs - R_robot - 0.4` | `||l+τv|| - R_obs - R_robot - β_i` |
| 安全余量 | 固定 `0.4 m` | `β_i = β_bar(c_i) μ(φ_i)` |
| 语义类别 | 不使用 | 使用 |
| 上下文交互 | 不使用 | heading、TTC、density |
| 可行性保护 | 无显式 Guard | `β_hat <= h_EE - η` |
| 特殊情形 | baseline | `β_i ≡ 0.4` 时退化为 baseline |

这个对比是论文叙事的关键：本文不是简单“多加一个安全距离”，而是把固定安全距离替换为可解释、可调节、可守护的语义安全余量。

---

# 四、系统框架

整体系统可以围绕两个规划级输出组织：

$$
\boxed{\beta_i}
$$

和：

$$
\boxed{\hat{\xi}_{i,1:H}}
$$

其中：

- `β_i` 表示 Guarded Semantic Safety Margin，告诉规划器“应该离它多远”。
- `\hat{\xi}_{i,1:H}` 表示障碍物预测轨迹，告诉规划器“它未来会在哪里”。

二者是并列输入，共同构成语义动态风险信息。

```text
Sensor Input
  ├── RGB-D Image
  ├── LiDAR Point Cloud
  └── IMU / Odometry

RGB-D Image
  → YOLOv8 Detection
  → Semantic Class c_i

LiDAR Point Cloud
  → Point Cloud Filter
  → Point Cloud Clustering
  → Obstacle Tracking
  → Trajectory Prediction
  → Obstacle Predicted Trajectory ξ̂_i

Semantic Class c_i + Dynamic Obstacle State
  → Semantic Dynamic Obstacle Representation
  → Semantic Margin Generation
  → Safety Guard
  → Guarded Semantic Margin β_i

β_i + ξ̂_i
  → SEESM Risk Evaluation
  → SEESM-Guided Global Path Searching
  → Reference Path

β_i + ξ̂_i + Reference Path + Robot State
  → MPC-SECBF Local Planner
  → Safe Local Trajectory
  → cmd_vel
```

建议在论文框图中增加一个中间模块：

```text
SEESM Risk Evaluation
```

它接收：

```text
β_i + ξ̂_i
```

然后分别给全局路径搜索和局部 MPC-SECBF 提供语义动态风险信息。这样图不会显得右侧连线过乱。

---

# 五、语义动态障碍物表征

本文的关键接口是语义动态障碍物：

$$
\mathcal{O}_t^{sem}
=
\left\{
id_i,\ c_i,\ p_i^o,\ v_i^o,\ R_i,\ \phi_{i,t},\ \hat{\xi}_{i,1:H}
\right\}_{i=1}^{N}
$$

它融合了两类信息：

1. 语义信息：
   - obstacle id
   - semantic class
   - confidence

2. 动态信息：
   - position
   - velocity
   - radius
   - predicted trajectory
   - context features

这个表征是感知层到规划层的接口。

---

# 六、代码模块设计

## 1. `yolo_detector_node`

作用：语义检测。

输入：

```text
/camera/color/image_raw
/camera/depth/image_raw
```

输出：

```text
/yolo_detections
```

内容：

```text
bbox
class_id
confidence
optional depth
```

它只回答：

> 这个东西是什么？

不在该节点中生成最终 `β_i`。

---

## 2. `dynamic_obstacle_node`

作用：动态障碍物提取、跟踪和预测。

输入：

```text
/lidar_points
/odom
```

处理：

```text
Point Cloud Filtering
Point Cloud Clustering
Obstacle Tracking
Trajectory Prediction
```

输出：

```text
/dynamic_obstacles
```

内容：

```text
id_i
p_i^o
v_i^o
R_i
predicted trajectory ξ̂_i
```

它回答：

> 障碍物在哪里、怎么动、未来往哪里走？

---

## 3. `semantic_obstacle_representation_node`

作用：将 YOLO 检测结果和动态障碍物状态绑定到同一个 obstacle id。

输入：

```text
/yolo_detections
/dynamic_obstacles
```

处理：

```text
2D-3D association
IoU + Hungarian matching
temporal voting
context feature extraction
group detection
```

输出：

```text
/semantic_dynamic_obstacles
```

内容：

```text
id_i
class c_i
position p_i^o
velocity v_i^o
radius R_i
context feature φ_i
predicted trajectory ξ̂_i
```

---

## 4. `semantic_margin_node`

作用：根据类别和上下文生成候选语义余量。

输入：

```text
/semantic_dynamic_obstacles
```

处理：

$$
\hat{\beta}_{i,t}
=
\bar{\beta}(c_i)\mu(\phi_{i,t})
$$

输出：

```text
/semantic_margin_candidates
```

内容：

```text
id_i
beta_hat_i
beta_bar_i
class_i
context_i
```

这一层优先使用解析规则，不需要训练网络。

---

## 5. `safety_guard_node`

作用：审查候选 `β_hat_i`，输出最终可用于规划的 `β_i`。

输入：

```text
/semantic_margin_candidates
/current_robot_state
/dynamic_obstacles
```

处理：

```text
accept
rate-limit
rollback
```

输出：

```text
/guarded_semantic_margin
```

内容：

```text
id_i
beta_i
guard_status
```

`guard_status` 建议记录：

```text
accepted
limited
rollback
```

这个字段可以用于统计 Guard 触发次数，作为实验指标。

---

## 6. `global_planner_node`

作用：SEESM-Guided Path Searching。

输入：

```text
/map
/robot_state
/goal
/semantic_dynamic_obstacles
/guarded_semantic_margin
```

数学输入：

$$
\mathcal{M}_t,\ X_t,\ X_g,\ \hat{\xi}_i,\ \beta_i
$$

处理：

```text
semantic risk evaluation
SEESM-guided path searching
replan trigger
```

输出：

```text
/reference_path
```

语义风险半径采用：

$$
R_i^{sem}
=
R_{obs,i}+R_{robot}+\beta_i
$$

注意：这部分是论文完整系统目标。当前代码主要已完成局部 MPC-SECBF 对 `β_i` 的接入，全局路径搜索对 `β_i` 的显式接入还应作为后续实现或实验补充。

---

## 7. `mpc_secbf_local_planner_node`

作用：局部 MPC-SECBF 轨迹优化。

输入：

```text
/reference_path
/robot_state
/semantic_dynamic_obstacles
/guarded_semantic_margin
```

核心安全约束：

$$
h_{SEE}(i,k)
=
\|p_{rel,i,k}+\tau v_{rel,i,k}\|_2
-R_{obs,i}
-R_{robot}
-\beta_i
$$

$$
h_{SEE}(i,k+1)
\geq
(1-\gamma)h_{SEE}(i,k)
$$

输出：

```text
/optimized_trajectory
/cmd_vel
```

---

# 七、当前代码对应关系

当前代码已经具备论文核心公式的局部闭环。

| 论文公式/模块 | 当前代码位置 | 状态 |
|---|---|---|
| `β_hat = β_bar(c) μ(φ)` | `planner/semantic_guard/src/beta_guard_node.cpp`、`beta_ground_truth_node.cpp` | 已有 |
| `Guard: β_hat <= h_EE - η` | `planner/semantic_guard/src/beta_guard_node.cpp`、`beta_ground_truth_node.cpp` | 已有 |
| Guard 中 `h_EE = ||p_rel + τv_rel|| - R_obs - R_robot` | `beta_guard_node.cpp`、`beta_ground_truth_node.cpp` | 已对齐 |
| `h_SEE = h_EE - β_i` | `planner/mpc_secbf/src/mpc_secbf.cpp` | 已有 |
| `R_robot` 参数化 | `planner/mpc_secbf/src/mpc_secbf_node.cpp`、`mpc_secbf.cpp` | 已对齐 |
| `h_{k+1} >= (1-γ) h_k` | `planner/mpc_secbf/src/mpc_secbf.cpp` | 已有 |
| 障碍物预测轨迹 | `planner/vomp_planner/traj_planner/include/obs_manager/obs_manager.hpp` | 已有 |
| 全局 ADSM 搜索 | `planner/vomp_planner/traj_planner/src/theta_astar.cpp` | 已有 |
| 全局搜索显式读取 `β_i` | 待接入 | 后续工作 |

当前最准确的代码叙事是：

> 语义安全余量和 Guard 已接入局部 MPC-SECBF；全局规划已有动态安全搜索能力，但显式语义余量 `β_i` 对全局搜索的作用仍需继续补充，或在论文中作为完整框架设计与扩展实验说明。

---

# 八、最小工程实现路线

为了快速跑通，不建议一开始就拆成过多节点。可以先采用最小闭环：

```text
1. semantic_fusion_node
2. beta_guard_node / beta_ground_truth_node
3. mpc_secbf_node
4. dynamic obstacle prediction
```

然后逐步扩展：

```text
固定 β
→ 类别 β
→ 上下文动态 β
→ Guard
→ MPC-SECBF
→ 全局 SEESM 风险搜索
```

---

# 九、代码实现优先级

## 第一步：离线验证语义余量

先不接 MPC，只打印：

```text
id
class
position
velocity
radius
phi
beta_hat
beta_after_guard
guard_status
```

确认：

```text
pedestrian → beta 合理
child      → beta 更大
cyclist    → beta 较大
vehicle    → beta 较大
box        → beta 较小
unknown    → 退化到 baseline
```

---

## 第二步：让 MPC 读入固定 β

先不接 YOLO，手动给每个 obstacle 一个固定 `β`：

```text
pedestrian: 0.3
child:      0.5
box:        0.1
unknown:    0.4
```

验证 MPC-SECBF 约束能工作，并和 `β_i ≡ 0.4` 的 baseline 对比。

---

## 第三步：接入动态 β

让 `/safety_margin/beta` 或 `/guarded_semantic_margin` 进入 MPC，替换固定 `β`。

验证：

```text
class change → beta change
relative motion change → beta change
Guard violation → rollback or rate-limit
```

---

## 第四步：接入全局规划

让 SEESM-Guided Path Searching 读取：

```text
β_i
ξ̂_i
```

全局搜索中使用：

$$
R_i^{sem}
=
R_{obs,i}+R_{robot}+\beta_i
$$

或把 `β_i` 作为风险代价权重，使全局路径提前避开语义高风险区域。

---

## 第五步：做实验

建议实验顺序：

```text
1. fixed-margin baseline
2. category-aware β
3. context-aware β
4. Guard ablation
5. SEESM global planner ablation
6. simulation
7. Gazebo
8. real-world
```

---

# 十、实验设计

## 1. Baseline 对比

建议至少比较：

```text
MPC-DC
MPC-DCBF
Zhang2026 fixed margin
MPC-SECBF without Guard
MPC-SECBF with Guard
MPC-SECBF + SEESM global search
```

其中 Zhang2026 fixed margin 可由：

$$
\beta_i \equiv 0.4
$$

实现。

---

## 2. 场景设计

建议场景：

```text
box crossing
adult crossing
child crossing
cyclist crossing
vehicle crossing
crowd/group crossing
narrow corridor
head-on encounter
```

---

## 3. 指标设计

现有代码已经能统计部分指标：

```text
trajectory length
travel time
mean linear velocity
mean angular velocity
velocity variance
angular velocity variance
collision times
minimum obstacle distance
```

建议新增或强调：

```text
minimum h_EE
minimum h_SEE
Guard rollback count
Guard trigger rate
average beta
maximum beta
MPC infeasible count
planning time
semantic comfort score
```

---

# 十一、论文结构建议

## 1. Introduction

说明动态避障中固定安全距离的不足，引出语义感知和社会合规避障需求。

核心论点：

> 不同类别障碍物需要不同安全余量，但语义余量不能无限增大，否则会破坏 MPC 可行性。

---

## 2. Related Work

可以分为：

```text
Dynamic obstacle avoidance
Control barrier function
Model predictive control
Semantic navigation
Socially aware navigation
```

---

## 3. Semantic Dynamic Obstacle Representation

介绍：

$$
\mathcal{O}_t^{sem}
$$

以及语义类别、动态状态、预测轨迹和上下文特征。

---

## 4. Guarded Semantic Safety Margin

介绍：

$$
\hat{\beta}_{i,k}
=
\bar{\beta}(c_i)\mu(\phi_{i,k})
$$

以及：

$$
\hat{\beta}_{i,k}
\leq
h_{EE}(i,k)-\eta
$$

这章是本文区别于“简单加安全距离”的关键。

---

## 5. MPC-SECBF Formulation

介绍：

$$
h_{EE}(i,k)
=
\|p_{rel,i,k}+\tau v_{rel,i,k}\|_2
-R_{obs,i}
-R_{robot}
$$

$$
h_{SEE}(i,k)
=
h_{EE}(i,k)-\beta_i
$$

$$
h_{SEE}(i,k+1)
\geq
(1-\gamma)h_{SEE}(i,k)
$$

同时给出 MPC 目标函数、运动学模型和控制约束。

---

## 6. SEESM-Guided Global Planning

介绍全局规划如何使用：

$$
R_i^{sem}
=
R_{obs,i}+R_{robot}+\beta_i
$$

或语义风险代价，提前避开高风险动态障碍物。

如果当前实现还没完全接入 `β_i`，论文里应明确这是完整框架的一部分，并在实验前完成代码闭环。

---

## 7. Experiments

围绕以下问题组织实验：

```text
Q1: 语义 β 是否能产生类别差异化避障？
Q2: 上下文 μ 是否能对动态交互风险作出响应？
Q3: Guard 是否降低 MPC 不可行次数？
Q4: 与固定安全距离 baseline 相比，安全性和效率是否更平衡？
Q5: 全局 SEESM 搜索是否减少局部死锁和急转？
```

---

# 十二、最终公式骨架

整篇论文最重要的公式可以收束为以下几条：

$$
\hat{\beta}_{i,k}
=
\bar{\beta}(c_i)\mu(\phi_{i,k})
$$

$$
h_{EE}(i,k)
=
\|p_{rel,i,k}+\tau v_{rel,i,k}\|_2
-R_{obs,i}
-R_{robot}
$$

$$
\beta_{i,k}
=
\mathrm{Guard}(\hat{\beta}_{i,k}, h_{EE}(i,k))
$$

$$
h_{SEE}(i,k)
=
h_{EE}(i,k)-\beta_{i,k}
$$

$$
h_{SEE}(i,k+1)
\geq
(1-\gamma)h_{SEE}(i,k)
$$

Zhang2026 baseline 是本文方法的特殊情形：

$$
\beta_{i,k}\equiv d_{safe}=0.4
$$

---

# 十三、最终总结

你的论文不是“YOLO + MPC”的简单拼接，而是一个语义安全规划框架：

> 通过语义动态障碍物表征，将类别、上下文和预测轨迹统一起来；通过 `β_hat = β_bar(c) μ(φ)` 生成语义安全余量；通过 Safety Guard 保证余量在线更新的可行性；最后通过 MPC-SECBF 和 SEESM 全局搜索实现差异化、动态化、可解释的安全避障。

当前代码已经完成了局部 MPC-SECBF 和 Guard 公式的主要闭环。下一步最值得补的是：

1. 将 `β_i` 显式接入全局搜索或风险代价。
2. 完善 Guard 触发次数、`h_EE/h_SEE` 最小值等实验指标。
3. 用 `β_i ≡ 0.4` 构造 Zhang2026 fixed-margin baseline。
4. 做 category-aware、context-aware 和 Guard ablation 实验。
