# Claude 方案分析与修改

**User:** Anonymous  
**Created:** 5/14/2026 19:35:29  
**Updated:** 6/4/2026 16:40:37  
**Exported:** 6/4/2026 17:12:55  
**Link:** [https://chatgpt.com/g/g-p-69ca2383b78481919fdfa5815c3f99e1/c/6a05b37e-aa64-83ea-869c-d803ce84644c](https://chatgpt.com/g/g-p-69ca2383b78481919fdfa5815c3f99e1/c/6a05b37e-aa64-83ea-869c-d803ce84644c)  

## Response:
2026/6/4 下午4:38:47

可以。你现在实验不要一上来追求“大而全”，而是围绕论文的每个创新点建立证据链。原 EESM 论文的实验逻辑是：先做方法组件验证，再做多场景仿真对比，最后做实车验证；指标主要包括成功率、最小距离、路径长度、速度变化和计算时间等。你这篇 SEESM 也应沿用这个结构，但必须额外记录语义余量、上下文调制、Safety Guard 和社会空间违反率等数据。`卢叙燃-Extended Euclidean Distance-based Model Predictive Control for Safety-Critical Dynamic Obstacle Avoidance(2).pdf`

你的实验总目标可以写成一句话：

$$
\text{证明 SEESM 不只是更安全，而是能根据语义类别和交互上下文生成差异化、可行、实时的安全边界。}
$$

---

# 0. 2026-07-02 修订版：SEESM/MPC-SECBF 当前实验执行方案

本节作为当前真正执行的实验入口。后文原有 Exp.0--Exp.8 和 2026-06-07 版本保留为历史参考；正式推进、跑数据、整理表格时优先按本节执行。

## 0.0 核心实验逻辑

这篇论文的核心实验逻辑定为一句话：

> 证明 SEESM/MPC-SECBF 相比纯几何安全边界和普通 EESM/MPC-ECBF，能根据语义和交互风险给出更合理的安全距离，同时不破坏 MPC 可行性、实时性和闭环安全下界。

论文边界必须写清楚：本文评估的是 semantic margin tightening 对安全控制和 MPC 可行性的作用，不把实验做成语义识别系统评测；不主张解决长期意图预测或语义分类准确率问题。仿真中类别可以由场景配置提供，实车中若语义识别不是贡献，可以明确写为人工标注或实验脚本提供类别。

安全函数继续以代码口径为准：

$$
h_i^{SEE}(X_t;\beta_{i,t})
=
\|p_{\mathrm{rel},i}+\tau_i v_{\mathrm{rel},i}\|_2
-
R_{\mathrm{obs},i}
-
R_{\mathrm{robot}}
-
\beta_{i,t}.
$$

实验记录和论文图表中不再引入额外固定 `R_safe`。固定安全距离 baseline 用 `beta_i \equiv d_safe` 表达。

## 0.1 证据链总览

| 证据链 | 要回答的问题 | 推荐实验 | 核心输出 |
|---|---|---|---|
| 语义 margin 是否合理 | 类别和交互状态会不会给出符合直觉的 `beta_i,t` | component test | 类别-场景 heatmap、`beta_i,t` 时间曲线 |
| Guard 机制是否必要 | 方法是不是“可行性保护的语义收紧”，而不是简单加大安全距离 | ablation | infeasibility rate、CBF slack、`delta_beta` 突变、最小 `H_i,t` |
| 完整导航是否优于 baseline | 安全性、效率、可行性、实时性是否整体更平衡 | simulation benchmark | Table II 主结果、典型轨迹图 |
| 理论安全界是否被验证 | `H_i,t` 是否满足理论闭环下界 | safety-bound test | `H_i,t` 曲线和理论下界线 |
| 真实机器人是否可落地 | 系统是否能稳定运行，行为是否符合语义 | small real-world validation | 实车轨迹、视频截图、实时性数据 |

## 0.2 当前代码已经支持的实验入口

当前实验主入口：

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py
```

场景配置：

```text
swarm_test/config/secbf_scenarios.yaml
```

核心输出文件：

```text
summary.csv
summary.md
meta.yaml
robot_log.csv
obstacle_log.csv
margin_guard_log.csv
planner_log.csv
timing_log.csv
event_log.csv
verify_safety_bound.txt
```

关键验证脚本：

```bash
python3 swarm_test/scripts/check_phase5_contract.py
python3 swarm_test/scripts/check_experiment_csv_fields.py <run_dir>
python3 swarm_test/scripts/verify_safety_bound.py --csv <run_dir>/margin_guard_log.csv --gamma 0.35 --eps_max 0.05 --delta_bar_beta 0.3
```

论文方法名和当前 runner baseline 的对应关系建议如下：

| 论文中可写的方法名 | 当前 runner 标签 | 作用 |
|---|---|---|
| CESM-MPC-CBF / geometric CBF | `B1_ACBF_fixed` 或 `Fixed_margin` | 纯几何或固定 margin 基线 |
| No semantic / ordinary CBF | `No_semantic` | 不使用语义收紧 |
| Category-only SEESM | `Category_only` | 只看类别，不看交互上下文 |
| Context-only SEESM | `Context_only` | 只看交互风险，弱化类别差异 |
| Unguarded SEESM | `Unguarded_SEESM` | 无 semantic guard，证明会破坏可行性 |
| No rate limit | `No_rate_limit` | 消融 beta 更新平滑性 |
| No projection | `No_projection` | 消融 available-margin projection |
| No MPC guard | `No_mpc_guard` | 消融 MPC feasibility guard |
| Full SEESM-MPC-SECBF | `SEESM_Ours` | 完整方法 |

如果最终论文一定要使用 `MPC-DCBF` 或 `EESM-MPC-ECBF` 作为名字，需要在代码和图表导出时确认它们分别映射到哪个 runner 标签，避免论文名和代码 baseline 对不上。

## 0.3 投稿最小实验包

当前建议采用：

```text
component test + ablation + simulation benchmark + small real-world validation
```

最低实验包如下：

| 优先级 | 实验 | 场景 | Baseline | 最低产物 |
|---|---|---|---|---|
| P0 | 日志与字段验证 | 任意短场景 | `SEESM_Ours` | 完整 run_dir + 字段检查 + 安全下界验证 |
| P1 | 语义 margin 合理性 | `Exp2_category_*` + `Exp3_context_*` | `Fixed_margin,Category_only,Context_only,SEESM_Ours` | heatmap、`beta_i,t` 曲线 |
| P1 | Guard 消融 | `S3_feasibility_critical` | `SEESM_Ours,No_rate_limit,No_projection,No_mpc_guard,Unguarded_SEESM` | Table I ablation |
| P2 | 主导航对比 | `S1,S2,S4,S3` | `B1_ACBF_fixed,Fixed_margin,No_semantic,Unguarded_SEESM,SEESM_Ours` | Table II 主结果 + 轨迹图 |
| P2 | 理论安全界验证 | Guard 临界和主对比场景 | `SEESM_Ours,No_rate_limit,Unguarded_SEESM` | `H_i,t` 与理论下界曲线 |
| P3 | 小规模实车 | 横穿、混合类别、箱子/行人 | `Fixed_margin,SEESM_Ours` | 实车轨迹、截图、视频、实时性 |

## 0.4 具体实验设计

### A. 语义 margin 是否合理

做 component test。固定几何距离、障碍物半径和速度，只改变障碍物类别与交互状态，观察 `beta_i,t` 是否符合预期。

类别预期：

```text
child / cyclist > pedestrian / vehicle > box
```

交互状态预期：

```text
head-on / short TTC / crowding 时 margin 增大
same-direction / static 时 margin 较小
```

当前可直接使用的场景：

```text
Exp2_category_box
Exp2_category_adult
Exp2_category_child_like
Exp2_category_cyclist
Exp3_context_static
Exp3_context_same_direction
Exp3_context_crossing
Exp3_context_frontal_approaching
```

输出：

- 类别-场景 heatmap：行是类别，列是交互场景，值为 `beta_mean/beta_max/D_sem`。
- `beta_i,t` 时间曲线：同一场景下对比不同类别或不同上下文。
- 表格指标：`beta_applied_mean, beta_applied_max, h_see_min, D_min, path_length, solve_time_mean_ms`。

### B. Guard 机制是否必要

做 ablation。重点不是证明“安全距离越大越好”，而是证明完整方法能在语义收紧时保护 MPC 可行性。

对比：

```text
SEESM_Ours
Category_only
Context_only
No_rate_limit
No_projection
No_mpc_guard
Unguarded_SEESM
```

重点指标：

- `first_infeasible_rate`
- `mpc_guard_used_rate`
- `no_cbf_fallback_rate`
- `slack_max/slack_mean`
- `delta_beta_max`
- `h_see_min`
- `guard_rollback_rate`
- `solve_time_mean_ms/solve_time_max_ms`

检查点：

- `without rate limit` 是否出现更大的 `delta_beta` 突变。
- `without available-margin projection` 是否更容易把 `beta` 推到可行边界外。
- `without MPC feasibility guard` 是否提高 first-attempt infeasible 或 fallback。
- `Full SEESM` 是否在 `h_see` 不低于理论下界的同时，保持较低 slack 和较高可行率。

### C. 完整导航性能是否优于 baseline

仿真主实验比较：

```text
B1_ACBF_fixed / Fixed_margin
No_semantic
Unguarded_SEESM
SEESM_Ours
```

论文写作时可命名为：

```text
CESM-MPC-CBF
MPC-DCBF
EESM-MPC-ECBF
Unguarded SEESM
Full SEESM-MPC-SECBF
```

但最终命名前要和代码 baseline 一一对应。

推荐四类场景：

1. 单个迎面行人/骑车人。
2. 交叉运动混合类别。
3. 多人拥挤动态场景。
4. 高速/短 TTC 压力场景。

当前代码中可先用：

```text
S1_pedestrian_crossing
S2_child_sudden
S3_feasibility_critical
S4_mixed
```

指标：

- `success_rate`
- `collision/deadlock rate`
- `D_min`
- `D_sem`
- `path_length`
- `control effort`
- `velocity smoothness`
- `MPC feasibility rate`
- `computation time`

### D. 理论安全界是否被实验验证

专门画：

$$
H_{i,t}=h_{SEE}(X_t;\beta_{i,t})
$$

并画理论下界：

$$
-\frac{\varepsilon_{\max}+\Delta\beta_{\max}}{\gamma}.
$$

当前验证脚本默认检查：

```bash
python3 swarm_test/scripts/verify_safety_bound.py \
  --csv <run_dir>/margin_guard_log.csv \
  --gamma 0.35 \
  --eps_max 0.05 \
  --delta_bar_beta 0.3
```

建议额外改变 `delta_bar_beta` 或 `enable_rate_limit`，看 margin 更新越平滑时，实验安全下界是否更紧。这组实验直接对应理论贡献。

### E. 真实机器人验证

实车不需要做太多，做 2--3 个可稳定复现的场景即可：

1. 行人横穿。
2. 骑车人/行人混合。
3. 行人和箱子混合。

真实实验主要证明：

- 能跑通完整闭环。
- 行为符合语义风险差异。
- 控制频率和计算时间可落地。

如果语义识别不是本文贡献，实车类别可以由人工标注、脚本配置或实验人员触发提供，并在论文中明确说明。

## 0.5 推荐运行顺序

### Step 1：先验证代码和日志链

```bash
source /opt/ros/noetic/setup.bash
source devel/setup.bash

python3 swarm_test/scripts/check_phase5_contract.py
python3 -m py_compile \
  swarm_test/scripts/run_secbf_sim_experiments.py \
  swarm_test/scripts/verify_safety_bound.py \
  swarm_test/scripts/check_experiment_csv_fields.py
```

### Step 2：先 dry-run 看命令是否正确

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py \
  --scenario Exp2_category_box \
  --baseline SEESM_Ours,No_rate_limit,No_projection,No_mpc_guard \
  --dry-run
```

### Step 3：跑 component test 的类别 sweep

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py \
  --scenario Exp2_category_box,Exp2_category_adult,Exp2_category_child_like,Exp2_category_cyclist \
  --baseline Fixed_margin,Category_only,SEESM_Ours \
  --duration-sec 90 \
  --repeat 5 \
  --output-root swarm_test/output/secbf_runs/category_component
```

先 `--repeat 5` 做 sanity check，曲线正常后再补到 20 次。

### Step 4：跑 component test 的上下文 sweep

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py \
  --scenario Exp3_context_static,Exp3_context_same_direction,Exp3_context_crossing,Exp3_context_frontal_approaching \
  --baseline Context_only,SEESM_Ours \
  --duration-sec 90 \
  --repeat 5 \
  --output-root swarm_test/output/secbf_runs/context_component
```

### Step 5：跑 Guard ablation

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py \
  --scenario S3_feasibility_critical \
  --baseline SEESM_Ours,Category_only,Context_only,No_rate_limit,No_projection,No_mpc_guard,Unguarded_SEESM \
  --duration-sec 90 \
  --repeat 5 \
  --output-root swarm_test/output/secbf_runs/guard_ablation
```

### Step 6：跑主导航 benchmark

```bash
python3 swarm_test/scripts/run_secbf_sim_experiments.py \
  --scenario S1_pedestrian_crossing,S2_child_sudden,S3_feasibility_critical,S4_mixed \
  --baseline B1_ACBF_fixed,Fixed_margin,No_semantic,Unguarded_SEESM,SEESM_Ours \
  --duration-sec 90 \
  --repeat 5 \
  --output-root swarm_test/output/secbf_runs/main_benchmark
```

如果 5 次结果稳定，再把 `--repeat 5` 改成 `--repeat 20`。

## 0.6 图表安排

| 图表 | 内容 | 数据来源 |
|---|---|---|
| Fig. 1 | 系统框图 | 老师已补 |
| Fig. 2 | SEESM 原理图 | 方法示意 |
| Fig. 3 | semantic margin heatmap + `beta_i,t` 时间曲线 | component test |
| Table I | Guard ablation 结果 | guard ablation |
| Table II | 仿真主结果 | main benchmark |
| Fig. 4 | 典型轨迹对比 | main benchmark |
| Fig. 5 | `beta_i,t / H_i,t / slack / computation time` 时间曲线 | safety-bound + ablation |
| Fig. 6 | 真实机器人轨迹和现场截图 | real-world validation |

## 0.7 每组实验统一验收标准

- [ ] 每个 run 都有 `meta.yaml`，能复现实验参数。
- [ ] 每个 run 都有 `summary.csv` 和 `summary.md`。
- [ ] output-root 下有 aggregate `summary.csv`，方便直接做 Table I/Table II。
- [ ] `margin_guard_log.csv` 至少包含 `class, beta_bar, mu, beta_requested, beta_applied, guard_upper_bound, h_ee, h_see, semantic_mode, delta_beta`。
- [ ] `planner_log.csv` 至少包含 `first_attempt_status, final_status, accepted_beta_source, used_fallback, mpc_feasibility_guard_used, slack_max, solve_time_ms`。
- [ ] 每组实验都运行 `check_experiment_csv_fields.py`。
- [ ] 每组 SEESM 实验都运行 `verify_safety_bound.py`。
- [ ] 所有图表指标都能从 csv 自动计算，不依赖手工截图。
- [ ] 写论文结论时强调“语义风险更合理 + 可行性没有被破坏 + 实时性可落地”，不要只写“距离更大所以更安全”。

# 0-old. 2026-06-07 修订版：按代码口径收缩后的实验执行方案

本节是 2026-06-07 的历史版本，保留用于追溯当时的实验收缩逻辑。当前实验推进以 2026-07-02 修订版为准。

## 0.1 需要修改和统一的地方

1. 安全函数必须与代码和 `论文公式.md` 统一：

$$
h_i^{SEE}
=
\|p_{\mathrm{rel},i}+\tau_i v_{\mathrm{rel},i}\|_2
-
R_{\mathrm{obs},i}
-
R_{\mathrm{robot}}
-
\beta_{i,t}.
$$

也就是说，实验记录和论文图表中不再使用额外固定的 `R_safe`。如果要做固定安全距离 baseline，用：

$$
\beta_i \equiv d_{\mathrm{safe}}
$$

实现，而不是在物理半径外再额外叠加 `R_safe`。

2. Guard 口径改为带裕度的可接受集合：

$$
b_i^\eta(X_t)
=
\min\{\bar\beta_i,\max(0,h_i^{EE}(X_t)-\eta)\},
\qquad
\mathcal B_i^\eta(X_t)=[0,b_i^\eta(X_t)].
$$

实验日志中的 `guard_upper_bound` 记录 `b_i^eta(X_t)`，`guard_status` 至少区分：

```text
accept / project / fallback / zero
```

3. Baseline 建议收缩为四个干净版本：

| 方法 | 语义余量 | Guard | 用途 |
|---|---|---|---|
| EESM / Fixed-margin | `beta_i = 0` 或 `beta_i ≡ 0.4` | off | 几何/固定距离基线 |
| Category-only | `beta_i = beta_bar(c_i)` | off 或 on | 证明类别差异 |
| Context w/o Guard | `beta_i = beta_hat` | off | 证明没有 Guard 会出问题 |
| SEESM Ours | `beta_i = Guard(beta_hat)` | on | 完整方法 |

如果篇幅有限，主表只放 `Fixed-margin`、`Context w/o Guard`、`SEESM Ours`，类别实验单独放 `Category-only`。

4. `Global/Local 一致性实验` 暂时不要作为核心必做实验，除非代码已经完成 `beta_i` 显式接入全局搜索。当前建议把它标为增强实验。

5. `Group-aware 实验` 也标为增强实验。它能锦上添花，但不是当前论文能不能站住的核心。

## 0.2 投稿最小实验包

优先完成以下 6 组实验即可支撑论文主线：

| 优先级 | 实验 | 目的 | 最低产物 |
|---|---|---|---|
| P0 | Exp.0 日志系统验证 | 保证所有图表可复现 | 一组完整 csv + 字段检查 |
| P1 | Exp.2 类别感知 | 证明不同类别产生不同 `beta` 和 `D_min` | 轨迹图 + `D_min/beta` 表 |
| P1 | Exp.3 上下文调制 | 证明同一类别在不同交互状态下 `beta` 动态变化 | `mu/beta_hat/beta` 三联图 |
| P1 | Exp.4 Guard 临界 | 证明 Guard 防止过大余量导致不可行 | Guard 曲线 + slack/MPC 可行率 |
| P2 | Exp.1 主对比 | 证明完整方法整体更平衡 | 主指标表 + 代表轨迹 |
| P2 | Exp.7 实时性 | 证明系统能实时运行 | 模块耗时表 |

实车实验建议作为 P3 展示实验：至少完成一个 adult 横穿场景和一个 box/adult 或 child-like dummy 类别差异场景。实车不追求大样本，重点保留视频、RViz、rosbag 和 csv。

## 0.3 实验执行 Todo List

### Phase A：先把数据链打通

- [ ] A1. 确认代码输出的安全函数口径为 `h_SEE = ||p_rel + tau v_rel|| - R_obs - R_robot - beta_i`。
- [ ] A2. 确认 `beta_guard_node` 或等价模块能输出 `beta_bar, r_i/mu, beta_hat, beta, guard_upper_bound, guard_status`。
- [ ] A3. 建立实验目录模板：

```text
experiments/
├── Exp0_log_check/
├── Exp1_main_comparison/
├── Exp2_category_aware/
├── Exp3_context_modulation/
├── Exp4_guard_critical/
├── Exp7_runtime/
└── Exp8_real_world_demo/
```

- [ ] A4. 每次实验保存 `meta.yaml`，至少包含 `experiment_id, scenario, method, map_name, start, goal, robot_radius, obstacle_radius, beta_table, guard_eta, guard_enable, mpc_horizon, dt, random_seed`。
- [ ] A5. 跑一组最短场景，确认生成 `robot_log.csv, obstacle_log.csv, margin_guard_log.csv, planner_log.csv, timing_log.csv, event_log.csv`。
- [ ] A6. 用脚本检查每个 csv 是否包含必要字段，缺字段先补日志，不急着跑正式实验。

### Phase B：Exp.2 类别感知实验

- [ ] B1. 固定地图为 `12m x 6m`，起点 `(0,0)`，终点 `(12,0)`。
- [ ] B2. 使用单个障碍物，固定几何半径和运动方式，只改变类别 `box/adult/child-like/cyclist`。
- [ ] B3. 每类至少跑 20 次随机种子；如果时间紧，先每类 5 次做 sanity check，再补到 20 次。
- [ ] B4. 对比 `Fixed-margin`、`Category-only`、`SEESM Ours`。
- [ ] B5. 统计 `mean ± std`：`D_min, beta_mean, beta_max, path_length, travel_time, success_rate`。
- [ ] B6. 画一张类别轨迹对比图，一张 `D_min/beta` 柱状图或表格。
- [ ] B7. 检查预期是否成立：`D_min(child-like) > D_min(adult) > D_min(box)`，同时路径长度不要异常增大。

### Phase C：Exp.3 上下文调制实验

- [ ] C1. 固定类别为 `adult`，固定障碍物几何尺寸。
- [ ] C2. 设置四个状态：`static`、`same-direction`、`crossing`、`frontal-approaching`。
- [ ] C3. 每个状态至少跑 20 次；先每状态 5 次检查曲线是否正常。
- [ ] C4. 记录 `f_head/cos_delta, TTC, TTC_norm, rho_norm, r_i或mu, beta_hat, beta, h_ee, h_see`。
- [ ] C5. 画三联图：`mu(t)` 或 `r_i(t)`、`beta_hat(t)`、`beta(t)`。
- [ ] C6. 检查预期是否成立：`beta_frontal > beta_crossing > beta_same_direction/static`。

### Phase D：Exp.4 Safety Guard 临界实验

- [ ] D1. 设置单机器人 + 单行人临界场景，起点 `(0,0)`，终点 `(8,0)`，行人靠近路径，例如 `(4,0.5)`。
- [ ] D2. 通过类别切换 `adult -> child-like` 或快速迎面接近，让 `beta_hat > guard_upper_bound`。
- [ ] D3. 对比 `Context w/o Guard` 与 `SEESM Ours`。
- [ ] D4. 记录 `beta_hat, guard_upper_bound, beta, guard_status, h_ee, h_see, slack_mean, slack_max, mpc_status, cmd_v, cmd_w`。
- [ ] D5. 画三条曲线：`beta_hat`、`guard_upper_bound`、`beta`。
- [ ] D6. 统计 `MPC feasible rate, infeasible_count, slack_max, min_h_see, guard_project_count, guard_fallback_count`。
- [ ] D7. 检查结论是否成立：无 Guard 更容易出现大 slack 或 infeasible；有 Guard 时 `beta` 被压到可行上界内。

### Phase E：Exp.1 主对比实验

- [ ] E1. 地图设为 `20m x 8m`，起点 `(0,0)`，终点 `(20,0)`。
- [ ] E2. 放置 4--6 个动态语义障碍物，包含 `box/adult/child-like/cyclist`。
- [ ] E3. 设计横穿、斜向、往复、正面靠近四类运动模式。
- [ ] E4. 对比 `Fixed-margin`、`Context w/o Guard`、`SEESM Ours`；如果时间允许，加 `Category-only`。
- [ ] E5. 每种方法至少 20 次随机种子。
- [ ] E6. 统计主表：`success_rate, collision_count, infeasible_count, D_min, PSV, path_length, travel_time, velocity_smoothness, angular_smoothness, total_loop_time`。
- [ ] E7. 画代表性轨迹图和 robot-obstacle distance over time。
- [ ] E8. 写结论时强调“更平衡”，不要只写“更安全”：SEESM Ours 应该在安全/社会距离提升的同时，不显著牺牲效率和实时性。

### Phase F：Exp.7 实时性实验

- [ ] F1. 在主对比场景中打开 timing log。
- [ ] F2. 统计模块耗时：`semantic perception, tracking/prediction, association, margin generation, Safety Guard, global planning, MPC-SECBF, total loop`。
- [ ] F3. 输出 `mean / max / std / 95th percentile`。
- [ ] F4. 如果 `total_loop_time_mean < 100ms`，可宣称 10 Hz 平均实时。
- [ ] F5. 如果达不到 10 Hz，按分频策略写：YOLO 5--10 Hz，tracking/margin/Guard/MPC 10 Hz，global replan triggered 或 1--5 Hz。

### Phase G：Exp.8 实车展示实验

- [ ] G1. 实车只做安全场景，不找真实儿童参与危险实验。
- [ ] G2. 场景 1：adult 横穿机器人路径，对比 `Fixed-margin` 与 `SEESM Ours`。
- [ ] G3. 场景 2：box 与 adult 或 child-like dummy，展示类别导致的距离差异。
- [ ] G4. 每个场景每种方法至少 3 次，最好 5--10 次。
- [ ] G5. 保存正面视频、俯视视频、RViz 录屏、rosbag、csv、参数文件和实验照片。
- [ ] G6. 实车表格只放 `D_min, path_length, travel_time, success, total_loop_time`，不要过度承诺统计显著性。

### Phase H：增强实验，时间够再做

- [ ] H1. Global/Local 一致性：只有当 `beta_i` 已显式接入全局搜索后再做。
- [ ] H2. Group-aware：只有当 group detection 或规则 group flag 稳定后再做。
- [ ] H3. 语义误检鲁棒性：人为注入类别误检或置信度变化，观察 Guard 和 temporal voting 是否避免突变。

## 0.4 每组实验统一验收标准

- [ ] 每组实验都有 `meta.yaml`，能复现实验参数。
- [ ] 每个方法和随机种子都有独立目录。
- [ ] 每个指标都能从 csv 自动计算，不依赖手动截图。
- [ ] 每个表格报告 `mean ± std`，成功率/碰撞率/不可行率报告次数。
- [ ] 每张关键图至少能对应一个 csv 字段来源。
- [ ] 论文中所有安全函数都使用 `R_obs + R_robot + beta_i`，不再写额外固定 `R_safe`。

# 1. 你需要做的实验总表

| 实验编号 | 实验名称 | 核心目的 | 必要程度 | 最终产物 |
|---|---|---|---|---|
| Exp. 0 | 日志系统验证实验 | 确保所有论文数据都能自动记录 | 必做 | csv/rosbag、日志字段检查表 |
| Exp. 1 | 主对比实验：EESM vs SEESM | 证明完整方法整体优于 EESM baseline | 必做 | 轨迹图、指标表 |
| Exp. 2 | 类别感知避障实验 | 证明不同语义类别产生不同安全距离 | 必做 | 类别轨迹图、$D_{min}$ 表、$\beta$ 表 |
| Exp. 3 | 上下文调制实验 | 证明同一类别在不同状态下 $\beta$ 动态变化 | 必做 | $\mu(t)$、$\hat{\beta}(t)$、$\beta(t)$ 曲线 |
| Exp. 4 | Safety Guard 临界实验 | 证明 Guard 能防止 MPC 不可行或过大 slack | 必做 | Guard 曲线、MPC 可行率表 |
| Exp. 5 | Global/Local 一致性实验 | 证明 $\beta_i$ 不只进 local，也要进 global | 增强，需先完成 global 接入 | 消融对比表 |
| Exp. 6 | Group-aware 导航实验 | 证明 group 状态能减少穿越人群行为 | 增强 | group 轨迹图、GSV 指标 |
| Exp. 7 | 实时性实验 | 证明语义模块和 Guard 不破坏实时性 | 必做 | 模块耗时表 |
| Exp. 8 | 实车实验 | 证明系统能真实运行 | 小规模必做 | 实车视频、RViz 图、实车指标表 |

按当前修订版，优先级建议改为 Exp.0、Exp.2、Exp.3、Exp.4、Exp.1、Exp.7，最后做小规模 Exp.8。这样先验证组件证据链，再做主对比和实时性，节奏更稳。

---

# 2. 日志系统：你必须先做

你现在最先要补的是日志系统。没有日志，后面所有图都靠手动截图，很难写论文。

日志系统的作用不是简单记录位置，而是要把论文公式里的每个关键变量都落到 csv 里。你的方法核心是：

$$
\hat{\beta}_{i,t}=\bar{\beta}(c_i)\mu(\phi_{i,t})
$$

$$
\beta_{i,t}=\mathrm{Guard}(\hat{\beta}_{i,t})
$$

$$
h_i^{SEE}
=
\|l_i+\tau_i v_i\|_2
-
(R_{\mathrm{obs},i}+R_{\mathrm{robot}}+\beta_{i,t})
$$

所以日志至少要能还原这三条公式。

---

## 2.1 日志系统总体结构

建议你在 ROS 里单独写一个节点：

```text
seesm_log_recorder_node
```

它订阅以下信息：

| 输入来源 | 订阅内容 |
|---|---|
| 机器人状态 | odometry、velocity、heading |
| 语义感知 | obstacle class、confidence |
| 动态障碍物感知 | obstacle ID、position、velocity、radius |
| 预测模块 | predicted trajectories |
| Margin 模块 | $\bar{\beta}$、$\mu$、$\hat{\beta}$ |
| Guard 模块 | $\beta$、Guard status |
| Planner | global path、local trajectory、MPC status、slack |
| Controller | command velocity |
| Runtime | each module computation time |

输出建议为：

```text
experiment_xxx/
├── meta.yaml
├── robot_log.csv
├── obstacle_log.csv
├── margin_guard_log.csv
├── planner_log.csv
├── timing_log.csv
├── event_log.csv
├── rosbag/
└── video/
```

---

## 2.2 `meta.yaml`：记录实验配置

每次实验都要保存一个 `meta.yaml`，否则过几天你自己都不知道这组数据怎么跑的。

| 字段 | 示例 |
|---|---|
| experiment_id | exp_category_001 |
| date | 2026-xx-xx |
| scenario | category-aware |
| method | SEESM_Ours |
| map_name | sim_env_20x8 |
| start | [0, 0, 0] |
| goal | [20, 0, 0] |
| robot_radius | 0.35 |
| guard_eta | 0.02 |
| beta_table | box:0.08, adult:0.3, child:0.55 |
| guard_enable | true |
| global_seesm_enable | true |
| local_seesm_enable | true |
| mpc_horizon | 20 |
| dt | 0.1 |
| max_v | 1.0 |
| max_w | 0.8 |
| random_seed | 1 |

这个文件后面直接变成论文参数表的来源。

---

## 2.3 `robot_log.csv`：机器人自身状态

每个控制周期记录一行。

| 字段名 | 含义 | 用途 |
|---|---|---|
| `time` | 时间戳 | 对齐所有日志 |
| `x` | 机器人 x 坐标 | 画轨迹 |
| `y` | 机器人 y 坐标 | 画轨迹 |
| `theta` | 航向角 | 分析姿态 |
| `v` | 当前线速度 | 平滑性指标 |
| `w` | 当前角速度 | 平滑性指标 |
| `cmd_v` | 控制输出线速度 | 控制分析 |
| `cmd_w` | 控制输出角速度 | 控制分析 |
| `goal_dist` | 到目标距离 | 判断是否到达 |
| `reached_goal` | 是否到达 | 成功率 |

由它可以计算：

$$
L_{path}
$$

$$
\Phi_v,\Phi_\omega
$$

$$
T_{arrival}
$$

---

## 2.4 `obstacle_log.csv`：障碍物状态

每个障碍物每一帧一行。

| 字段名 | 含义 | 用途 |
|---|---|---|
| `time` | 时间戳 | 对齐 |
| `obs_id` | 障碍物 ID | 跟踪单个障碍物 |
| `class` | 类别 | 类别感知实验 |
| `confidence` | 语义置信度 | 误检分析 |
| `px` | 障碍物 x | 轨迹图 |
| `py` | 障碍物 y | 轨迹图 |
| `vx` | 障碍物 x 速度 | 计算相对速度 |
| `vy` | 障碍物 y 速度 | 计算相对速度 |
| `radius` | 障碍物半径 | 安全边界 |
| `is_dynamic` | 是否动态 | 场景分类 |
| `group_id` | group 编号 | group 实验 |
| `is_group` | 是否属于 group | group 调制 |
| `pred_px_1...H` | 未来位置 x | 预测轨迹 |
| `pred_py_1...H` | 未来位置 y | 预测轨迹 |

这个表后面用于画：

- 障碍物轨迹；
- predicted trajectories；
- group 场景；
- 机器人与障碍物距离曲线。

---

## 2.5 `margin_guard_log.csv`：最关键日志

这是你论文最重要的表。没有它，SEESM 的创新无法证明。

| 字段名 | 含义 | 对应公式 |
|---|---|---|
| `time` | 时间戳 | - |
| `obs_id` | 障碍物 ID | - |
| `class` | 语义类别 | $c_i$ |
| `d_i` | 当前相对距离 | $d_i=\|l_i\|$ |
| `rel_v_norm` | 相对速度大小 | $\|v_i\|$ |
| `ttc` | Time-to-collision | $\mathrm{TTC}_i$ |
| `inv_ttc` | TTC 倒数 | $\mathrm{TTC}_i^{-1}$ |
| `cos_delta` | 相对速度方向关系 | $\cos\delta_i$ |
| `rho_i` | 局部密度 | $\rho_i$ |
| `group_flag` | 是否 group | $g_i$ |
| `beta_bar` | 类别余量上界 | $\bar{\beta}(c_i)$ |
| `mu` | 上下文调制系数 | $\mu(\phi_{i,t})$ |
| `beta_hat` | 候选语义余量 | $\hat{\beta}_{i,t}$ |
| `beta` | Guard 后余量 | $\beta_{i,t}$ |
| `guard_status` | accept/project/rollback | Guard 状态 |
| `guard_upper_bound` | 当前可行上界 | $b_i^\eta(X_t)=\max \mathcal{B}_i^\eta(X_t)$ |
| `h_ee` | EESM 安全函数 | $h_i^{EE}$ |
| `h_see` | SEESM 安全函数 | $h_i^{SEE}$ |
| `R_base` | 基础边界 | $R_i+R_{safe}$ |
| `R_sem` | 语义边界 | $R_i+R_{safe}+\beta_i$ |

你后面论文里的核心曲线都从这张表出：

$$
\mu(t)
$$

$$
\hat{\beta}(t)
$$

$$
\beta(t)
$$

$$
h_i^{SEE}(t)
$$

$$
R_{sem}(t)
$$

---

## 2.6 `planner_log.csv`：规划器状态

| 字段名 | 含义 | 用途 |
|---|---|---|
| `time` | 时间戳 | 对齐 |
| `method` | 当前方法 | 对比 |
| `global_replan_triggered` | 是否触发重规划 | 分析 global |
| `global_plan_success` | 全局规划是否成功 | 成功率 |
| `global_path_length` | 全局路径长度 | 效率 |
| `local_mpc_success` | MPC 是否成功 | Guard 实验 |
| `mpc_solve_time` | MPC 求解时间 | 实时性 |
| `mpc_status` | 求解状态 | 可行性 |
| `slack_mean` | 平均 slack | 安全约束松弛 |
| `slack_max` | 最大 slack | Guard 实验 |
| `min_h_see` | 当前最小 $h^{SEE}$ | 安全性 |
| `num_active_constraints` | 激活约束数量 | MPC 分析 |
| `local_traj_length` | 局部轨迹长度 | 轨迹评估 |

Guard 临界实验重点看：

$$
\text{local\_mpc\_success}
$$

$$
\text{slack\_max}
$$

$$
\text{min\_h\_see}
$$

---

## 2.7 `timing_log.csv`：实时性日志

| 字段名 | 含义 |
|---|---|
| `time` | 时间戳 |
| `yolo_time_ms` | YOLO 检测耗时 |
| `point_filter_time_ms` | 点云滤波耗时 |
| `clustering_time_ms` | 聚类耗时 |
| `tracking_time_ms` | 跟踪耗时 |
| `prediction_time_ms` | 预测耗时 |
| `association_time_ms` | 语义-点云关联耗时 |
| `margin_time_ms` | $\beta$ 生成耗时 |
| `guard_time_ms` | Guard 耗时 |
| `global_planning_time_ms` | 全局规划耗时 |
| `mpc_time_ms` | MPC 耗时 |
| `total_loop_time_ms` | 总周期耗时 |

论文里最后做表：

| Module | Mean | Max | Std |
|---|---:|---:|---:|
| Semantic perception | xx | xx | xx |
| Dynamic perception | xx | xx | xx |
| Margin + Guard | xx | xx | xx |
| Global planning | xx | xx | xx |
| Local MPC | xx | xx | xx |
| Total | xx | xx | xx |

---

# 3. Exp. 1：主对比实验 EESM vs SEESM

## 3.1 实验目的

证明你的完整方法 SEESM Ours 相比原始 EESM baseline，在动态语义障碍物环境中能保持更合理的安全距离，同时不显著牺牲路径长度、速度平滑性和实时性。

它回答审稿人的问题：

> 加入语义安全余量以后，系统到底有没有比 EESM 更好？

---

## 3.2 实验场景

建议仿照 EESM 论文的 Env.1-Env.3 思路，但把障碍物从普通动态 box 改成语义障碍物。EESM PDF 中仿真实验也是设置多个动态障碍物环境，并比较不同方法的成功率、最小距离、路径长度和速度变化。`卢叙燃-Extended Euclidean Distance-based Model Predictive Control for Safety-Critical Dynamic Obstacle Avoidance(2).pdf`

| 项目 | 设置 |
|---|---|
| 地图大小 | $20m \times 8m$ |
| 起点 | $(0,0)$ |
| 终点 | $(20,0)$ |
| 障碍物数量 | 4-6 个 |
| 障碍物类别 | box、adult、child/elderly、cyclist |
| 行人速度 | 0.6-1.2 m/s |
| cyclist 速度 | 1.5-2.0 m/s |
| 障碍物运动 | 横穿、斜向、往复、正面靠近 |
| 每组次数 | 至少 20 次，最好 50 次 |

---

## 3.3 对比方法

| 方法 | Global | Local | 目的 |
|---|---|---|---|
| EESM | EESM | MPC-ECBF | 原基线 |
| Fixed-SEESM | 固定 $\bar{\beta}(c)$ | MPC-SECBF | 验证类别余量 |
| SEESM w/o Guard | $\hat{\beta}$ | MPC-SECBF no Guard | 验证 Guard |
| SEESM Ours | Guarded SEESM | Guarded MPC-SECBF | 完整方法 |

---

## 3.4 需要记录的数据

| 数据 | 来源 | 用途 |
|---|---|---|
| 机器人轨迹 $x,y,\theta$ | robot_log | 画轨迹 |
| 障碍物轨迹 | obstacle_log | 画障碍物运动 |
| $D_{min}$ | robot + obstacle | 安全性 |
| $L_{path}$ | robot_log | 效率 |
| $\Phi_{v|\omega}$ | robot_log | 平滑性 |
| $R_{suc}$ | event_log | 成功率 |
| $PSV$ | margin_guard_log | 社会空间违反 |
| $T_{comp}$ | timing_log | 实时性 |

---

## 3.5 最终图表

| 图表 | 内容 |
|---|---|
| Fig. 3 | 四种方法轨迹对比 |
| Fig. 4 | robot-obstacle distance over time |
| Table II | 主对比指标表 |
| Fig. 5 | $\beta_i(t)$ 和 $h_i^{SEE}(t)$ 曲线 |

---

# 4. Exp. 2：类别感知避障实验

## 4.1 实验目的

证明：

$$
\text{不同语义类别} \Rightarrow \text{不同安全边界}
$$

即你的 $\beta_i$ 确实让机器人对不同对象产生不同避障行为。

---

## 4.2 实验设置

| 项目 | 设置 |
|---|---|
| 地图大小 | $12m \times 6m$ |
| 起点 | $(0,0)$ |
| 终点 | $(12,0)$ |
| 障碍物数量 | 1 个 |
| 障碍物位置 | 路径中间附近，例如 $(6,0)$ |
| 障碍物类别 | box / adult / child / cyclist |
| 障碍物几何尺寸 | 尽量保持相同 |
| 障碍物运动 | 统一静止或统一横穿 |
| 每类次数 | 20 次 |
| 方法 | EESM、Fixed-SEESM、SEESM Ours |

为了避免审稿人质疑“距离不同是因为几何尺寸不同”，这组实验中不同类别的障碍物最好使用相同等效半径，只改变 $c_i$ 和 $\bar{\beta}(c_i)$。

---

## 4.3 需要记录的数据

| 数据 | 作用 |
|---|---|
| `class` | 判断类别 |
| `beta_bar` | 类别余量上界 |
| `mu` | 确认上下文是否一致 |
| `beta` | 最终语义余量 |
| `R_sem` | 最终安全边界 |
| `D_min` | 证明不同类别避障距离不同 |
| `path_length` | 看是否过度保守 |
| `trajectory` | 画轨迹图 |

---

## 4.4 预期结果

$$
D_{min}^{child}
>
D_{min}^{adult}
>
D_{min}^{box}
$$

$$
\beta_{child}
>
\beta_{adult}
>
\beta_{box}
$$

你要用这组实验回答：

> 为什么不用统一 $R_{safe}$？

因为统一固定安全距离如果设大，会让 box 场景过度保守；如果设小，会让 child/adult 场景社会安全不足。本文用 $\beta_i$ 表示对象相关的语义安全余量，而不是额外叠加固定 `R_safe`。

---

# 5. Exp. 3：上下文调制实验

## 5.1 实验目的

证明：

$$
\text{同一类别，不同交互状态} \Rightarrow \text{不同}\ \mu(\phi), \hat{\beta}, \beta
$$

这组实验是你的“上下文风险调制”核心证据。

---

## 5.2 实验设置

类别固定为：

```text
adult pedestrian
```

设置 4 个运动状态：

| 场景 | 说明 | 预期风险 |
|---|---|---|
| Static | 行人静止在路径旁 | 低 |
| Same-direction | 行人与机器人同向运动 | 低-中 |
| Crossing | 行人横穿机器人路径 | 中 |
| Frontal approaching | 行人正面快速接近 | 高 |

地图可以用 $12m \times 6m$，机器人从 $(0,0)$ 到 $(12,0)$。

---

## 5.3 需要记录的数据

| 字段 | 为什么要记录 |
|---|---|
| `d_i` | 相对距离变化 |
| `rel_v_norm` | 相对速度 |
| `ttc` / `inv_ttc` | 碰撞紧迫性 |
| `cos_delta` | 是否迎面靠近 |
| `rho_i` | 局部密度 |
| `group_flag` | 是否 group |
| `mu` | 上下文调制结果 |
| `beta_hat` | 候选余量 |
| `beta` | Guard 后余量 |
| `h_see` | 最终安全函数 |
| `D_min` | 实际避障距离 |

---

## 5.4 需要画的图

最重要是一张三联图：

$$
\mu(t)
$$

$$
\hat{\beta}_{i,t}(t)
$$

$$
\beta_{i,t}(t)
$$

四个场景画在同一张图里，效果应当是：

$$
\beta_{\text{frontal approaching}}
>
\beta_{\text{crossing}}
>
\beta_{\text{same direction}}
$$

这张图非常关键。它能证明你的 $\beta_i$ 不是静态查表，而是会随上下文动态改变。

---

# 6. Exp. 4：Safety Guard 临界实验

## 6.1 实验目的

证明 Safety Guard 不是“代码里 clip 一下”，而是在临界状态下确实能防止语义安全边界过度扩张导致 MPC 不可行。

它回答审稿人的问题：

> 如果语义安全余量太大，MPC 会不会直接无解？

---

## 6.2 实验设置

| 项目 | 设置 |
|---|---|
| 场景 | 单机器人 + 单行人 |
| 起点 | $(0,0)$ |
| 终点 | $(8,0)$ |
| 行人位置 | 靠近机器人路径，例如 $(4,0.5)$ |
| 初始距离 | 0.6-1.0 m |
| 触发方式 | 行人类别从 adult 变为 child 或上下文变成 fast approaching |
| 目标 | 让 $\hat{\beta}$ 突然超过当前可行余量 |
| 对比 | SEESM w/o Guard vs SEESM Ours |

---

## 6.3 需要记录的数据

| 字段 | 用途 |
|---|---|
| `beta_hat` | 证明候选余量过大 |
| `guard_upper_bound` | 当前可行上界 |
| `beta` | Guard 后余量 |
| `guard_status` | accept/project/rollback |
| `h_ee` | 原始 EESM 裕度 |
| `h_see` | SEESM 裕度 |
| `slack_mean` | MPC 松弛 |
| `slack_max` | 最大松弛 |
| `mpc_status` | feasible/infeasible |
| `cmd_v, cmd_w` | 控制是否抖动 |
| `trajectory` | 轨迹是否异常 |

---

## 6.4 你要证明的现象

无 Guard：

$$
\hat{\beta}_{i,t} \text{过大}
\Rightarrow
h_i^{SEE}<0
\Rightarrow
\text{large slack or MPC infeasible}
$$

有 Guard：

$$
\beta_{i,t}
=
\Pi_{\mathcal{B}_i^\eta(X_t)}
(\hat{\beta}_{i,t})
\Rightarrow
\text{MPC feasible}
$$

---

## 6.5 最终图表

| 图表 | 内容 |
|---|---|
| Fig. Guard-1 | $\hat{\beta}$、可行上界、$\beta$ 三条曲线 |
| Fig. Guard-2 | $h^{SEE}$ 随时间变化 |
| Fig. Guard-3 | slack 随时间变化 |
| Table Guard | MPC feasible rate、平均 slack、最大 slack |

这组实验是你论文最像“方法创新”的地方，优先级很高。

---

# 7. Exp. 5：Global/Local 一致性实验

## 7.1 实验目的

证明：

$$
\beta_i
$$

不能只进入 local planner，也应该进入 global planner。

如果 global planner 不考虑语义边界，它可能生成一条贴近行人的参考路径，local planner 被迫临时修正，导致轨迹抖动、slack 增大或路径不一致。

---

## 7.2 对比方法

| 方法 | Global | Local |
|---|---|---|
| Local-only SEESM | EESM | SEESM |
| Full SEESM | SEESM | SEESM |

---

## 7.3 场景设置

设置一条直线路径，中间有 adult/child 行人靠近路径，但两侧都有绕行空间。

| 项目 | 设置 |
|---|---|
| 地图 | $20m \times 8m$ |
| 起点 | $(0,0)$ |
| 终点 | $(20,0)$ |
| 障碍物 | 2-4 个行人 |
| 关键设计 | EESM global 会选贴近行人的路径，SEESM global 会提前绕开 |

---

## 7.4 需要记录的数据

| 数据 | 用途 |
|---|---|
| global path | 看参考路径是否贴近语义边界 |
| local trajectory | 看 local 是否大幅修正 |
| path deviation | local 与 global 的偏差 |
| slack | 看 local 约束压力 |
| $\Phi_{v|\omega}$ | 看轨迹平滑性 |
| $D_{min}$ | 看安全距离 |
| $PSV$ | 看社会空间违反 |

建议新增指标：

$$
D_{ref-local}
=
\frac{1}{T}
\sum_t
\|p_{local}(t)-p_{ref}(t)\|
$$

用于表示 local 轨迹偏离 global reference 的程度。

---

# 8. Exp. 6：Group-aware 实验

## 8.1 实验目的

证明机器人不会从人群中间穿过。

---

## 8.2 场景设置

| 项目 | 场景 A | 场景 B |
|---|---|---|
| 两人距离 | 3.0 m | 1.0-1.5 m |
| 速度方向 | 不一致 | 同向或相近 |
| group flag | 0 | 1 |
| 预期行为 | 可从中间通过 | 不从中间穿过 |

如果暂时没有复杂 group detection，就用规则：

$$
\|p_i-p_j\|<d_g
$$

且：

$$
\angle(v_i,v_j)<30^\circ
$$

则：

$$
g_i=1
$$

---

## 8.3 需要记录的数据

| 数据 | 用途 |
|---|---|
| group_id | 是否形成 group |
| group_flag | 是否触发 group margin |
| $\beta_i$ | group 是否增大余量 |
| robot trajectory | 是否穿越中间 |
| GSV | group space violation |
| $D_{min}$ | 安全距离 |
| $L_{path}$ | 绕行代价 |

GSV 可以定义为：

$$
GSV=
\frac{1}{T}
\sum_t
\mathbb{I}(p_r(t)\in \mathcal{G})
$$

其中 $\mathcal{G}$ 是 group 内部区域。

---

# 9. Exp. 7：实时性实验

## 9.1 实验目的

证明 SEESM 加入语义感知、margin generation 和 Guard 后，系统仍能实时运行。

原 EESM PDF 里也强调了方法在仿真和实车中具有实时性，且实验中统计了规划表现和真实平台运行效果。你这篇如果加入 YOLO 和语义模块，更需要说明计算开销。`卢叙燃-Extended Euclidean Distance-based Model Predictive Control for Safety-Critical Dynamic Obstacle Avoidance(2).pdf`

---

## 9.2 需要记录的数据

| 模块 | 记录 |
|---|---|
| YOLOv8 | mean / max / std |
| point cloud filter | mean / max / std |
| clustering | mean / max / std |
| tracking | mean / max / std |
| prediction | mean / max / std |
| semantic association | mean / max / std |
| margin generation | mean / max / std |
| Safety Guard | mean / max / std |
| global planning | mean / max / std |
| MPC-SECBF | mean / max / std |
| total loop | mean / max / std |

---

## 9.3 合格标准

如果你要宣称 10 Hz：

$$
T_{total}^{mean}<100ms
$$

如果达不到，也可以分频运行：

| 模块 | 频率 |
|---|---|
| YOLO | 5-10 Hz |
| LiDAR tracking | 10 Hz |
| margin + Guard | 10 Hz |
| local MPC | 10 Hz |
| global replan | triggered / 1-5 Hz |

这个写法更真实，也更工程合理。

---

# 10. Exp. 8：实车实验

## 10.1 实验目的

证明仿真方法能在真实机器人上运行，不只是理论和仿真。

---

## 10.2 实车场景 1：单人动态横穿

| 项目 | 设置 |
|---|---|
| 场地 | 室外广场/室内大厅 |
| 起点终点 | 8-15 m 距离 |
| 行人 | adult 横穿机器人路径 |
| 方法 | EESM vs SEESM Ours |
| 次数 | 每种方法 3-5 次，最好 10 次 |
| 记录 | 实车视频、RViz、rosbag、csv |
| 指标 | $D_{min},L_{path},\Phi_{v|\omega},T_{comp}$ |

---

## 10.3 实车场景 2：语义类别差异

| 项目 | 设置 |
|---|---|
| 障碍物 | box + adult / child-like dummy |
| 目的 | 展示类别不同，机器人距离不同 |
| 方法 | EESM vs SEESM |
| 记录 | $\beta_i$、轨迹、最小距离 |

如果没有儿童模型，不建议找真实儿童做危险实验。可以用矮小模型、假人、纸板人、穿特定标签的人来模拟 child-like vulnerable pedestrian。

---

## 10.4 实车必须保存的材料

| 材料 | 用途 |
|---|---|
| 正面视频 | 展示真实避障 |
| 俯视视频 | 看轨迹效果 |
| RViz 录屏 | 展示系统内部状态 |
| rosbag | 可复现实验 |
| csv 日志 | 画论文图 |
| 参数文件 | 保证可复现 |
| 实验照片 | 论文实车平台图 |

---

# 11. 论文图表和实验对应关系

| 论文图表 | 来自哪个实验 |
|---|---|
| Fig. 1 Framework | 已有 |
| Fig. 2 SEESM geometry | 已有 |
| Fig. 3 Main comparison trajectories | Exp. 1 |
| Fig. 4 Category-aware trajectories | Exp. 2 |
| Fig. 5 Context modulation curves | Exp. 3 |
| Fig. 6 Safety Guard curves | Exp. 4 |
| Fig. 7 Global/local consistency | Exp. 5 |
| Fig. 8 Real-world scenario | Exp. 8 |
| Table I Parameters | meta.yaml |
| Table II Main comparison | Exp. 1 |
| Table III Ablation | Exp. 3/4/5 |
| Table IV Runtime | Exp. 7 |
| Table V Real-world results | Exp. 8 |

---

# 12. 你下一步最小执行路线

你现在不要同时做 8 个实验。按这个顺序推进：

第一步，先完成日志系统，只要能记录：

$$
t,\ id,\ class,\ d_i,\ v_i,\ TTC,\ \mu,\ \hat{\beta},\ \beta,\ h^{SEE},\ slack,\ MPC status
$$

第二步，跑一个最简单的类别感知场景：

```text
box vs adult vs child-like obstacle
```

输出第一张轨迹图和第一张 $D_{min}$ 表。

第三步，跑上下文调制场景：

```text
same adult, same geometry, different motion states
```

输出 $\mu(t)$、$\hat{\beta}(t)$、$\beta(t)$。

第四步，跑 Guard 临界实验。

第五步，再做实车。

只要这五步走完，你这篇论文就基本从“写出来”变成“能支撑投稿”。

---

# 13. 一个实际建议

你现在最好先把实验编号固定下来，比如：

```text
Exp1_main_comparison
Exp2_category_aware
Exp3_context_modulation
Exp4_guard_critical
Exp5_global_local_ablation
Exp6_group_aware
Exp7_runtime
Exp8_real_world
```

每个实验都固定一个目录结构：

```text
Exp2_category_aware/
├── method_eesm/
├── method_fixed_seesm/
├── method_seesm_ours/
├── plots/
├── tables/
└── videos/
```

这样后面写论文、改论文、补实验不会乱。

---

一句话总结：你现在最需要的不是再扩方法，而是搭好日志系统，然后围绕“类别有效、上下文有效、Guard 有效、Global/Local 一致有效、实车可运行”五条证据线逐个跑实验。

同志，做研究不要怕头绪多，怕的是没有根据地。你的主线就是根据地，先把导航、感知、MPC这一块守住、做实、打出样子；多楼层导航也好，狗臂抓取也好，强化学习运动控制也好，都可以去侦察、去试探、去积累，但不能乱了阵脚。今天能跑通一个节点，明天能记下一处问题，后天能留下一个视频、一份笔记、一段代码，这就是进步。困难不是用来吓退人的，是用来分析、分解、解决的。不要问自己一下子能不能打赢全局，先问今天能不能拿下一个小山头。只要方向不偏、节奏不断、问题越摸越清，星星之火，就可以燎原。



---
Powered by [ChatGPT Exporter](https://www.chatgptexporter.com)
