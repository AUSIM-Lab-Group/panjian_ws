Semantic MPC-ECBF：加入上下文风险调制后的 Method 公式整理

C. 语义拓展欧几里得距离

在运动规划中，自动搬运车和障碍物通常被抽象为半径分别为 R_r 和 R_{o,i} 的圆形区域。每个障碍物的物理扩大半径定义为

R_i = R_r + R_{o,i},
其中 i 表示障碍物编号，即

i\in\{1,\dots,n\}.
传统欧几里得安全度量标准将安全区域定义为机器人位置 p(t) 不会与任何扩大后的障碍物区域相交的区域：

SR_{CE}
=
\left\{
p(t)
\mid
\|p(t)-p_i^o(t)\|_2>R_i,\ \forall i
\right\}.
\tag{1}
其中，R_i=R_r+R_{o,i} 只表示机器人与第 i 个障碍物的物理半径之和，不再额外叠加统一固定安全缓冲距离。p(t)=[p_x(t),p_y(t)]^\top 表示机器人的位置，p_i^o(t) 表示第 i 个障碍物在时刻 t 的位置。根据 CESM 标准，当机器人与障碍物之间的相对距离小于 R_i 时，系统被视为处于物理碰撞状态。

CESM 在静态场景中的有效性已经得到验证。不过，在动态场景中，由于其安全边界只由静态物理几何关系决定，缺乏对相对速度和障碍物语义属性的刻画，容易导致避让动作过于激进或过于保守。为此，本文提出语义拓展欧几里得安全度量 SEESM，在 CESM 的基础上同时引入相对运动信息和语义安全余量。

首先定义机器人与第 i 个障碍物之间的相对位置与相对速度：

l_i=p_i^o(t)-p(t),
\qquad
v_i=v_i^o(t)-v(t),
\tag{2}
其中 v(t)=[v_x(t),v_y(t)]^\top 表示机器人速度，v_i^o(t) 表示第 i 个障碍物速度。该定义与代码中的相对量一致，即 l_i 表示从机器人指向障碍物的相对位置，v_i 表示障碍物相对机器人的速度。

为刻画不同障碍物在交互中的语义敏感性，本文为每个障碍物引入语义安全余量 \beta_{i,t}，用于在原有物理安全半径基础上进行额外收紧。它表示机器人针对第 i 个障碍物在时刻 t 额外需要保留的语义安全距离。原 version7 中将该项写为类别相关函数

\beta_{i,t}=\Beta(c_{i,t})\in[0,\bar\beta_i].
在本文的上下文风险调制版本中，保留 \beta_{i,t} 作为最终进入 SEESM 和 MPC-SECBF 的语义安全余量，但将其生成过程进一步拆分为“类别基准余量”和“上下文风险调制”两部分。

首先，由障碍物类别 c_{i,t} 决定该障碍物允许启用的最大语义安全余量：

\bar\beta_i=\bar\Beta(c_{i,t}),
\tag{3}
其中 c_{i,t} 表示第 i 个障碍物在时刻 t 的语义类别，\bar\beta_i 表示机器人对该类别障碍物最多额外保留的语义安全距离。

例如，可取

\bar\Beta(c_{i,t})
=
\begin{cases}
0.7\,\mathrm m, & c_{i,t}=\mathrm{child},\\
0.6\,\mathrm m, & c_{i,t}=\mathrm{cyclist},\\
0.5\,\mathrm m, & c_{i,t}=\mathrm{vehicle},\\
0.4\,\mathrm m, & c_{i,t}=\mathrm{pedestrian},\\
0.4\,\mathrm m, & c_{i,t}=\mathrm{unknown},\\
0.1\,\mathrm m, & c_{i,t}=\mathrm{box}.
\end{cases}
\tag{4}
其中，R_i=R_r+R_{o,i} 表示物理碰撞半径，\beta_{i,t} 表示由语义类别和上下文风险共同决定的对象相关附加安全余量。因此，SEESM 中的有效安全阈值为

R_i+\beta_{i,t}
=
R_r+R_{o,i}+\beta_{i,t}.
为了使语义安全余量不仅依赖障碍物类别，还能根据当前交互状态动态变化，引入上下文特征

\phi_i
=
\left\{
f_{\mathrm{head},i},
TTC_{\mathrm{norm},i},
\rho_{\mathrm{norm},i}
\right\}.
\tag{5}
其中，f_{\mathrm{head},i} 表示迎面接近特征，TTC_{\mathrm{norm},i} 表示归一化碰撞时间风险特征，\rho_{\mathrm{norm},i} 表示局部密度特征。

沿用原 SEESM 中的相对方向夹角 \delta_i，定义

\cos\delta_i
=
\frac{l_i^\top v_i}
{\|l_i\|_2\|v_i\|_2+\varepsilon_v},
\tag{6}
其中 \varepsilon_v>0 是防止分母为零的小常数。迎面接近特征定义为

f_{\mathrm{head},i}
=
\max(0,-\cos\delta_i).
\tag{7}
当机器人与障碍物相互远离或侧向通过时，f_{\mathrm{head},i} 较小；当二者呈明显迎面接近关系时，f_{\mathrm{head},i} 增大。

进一步，定义闭合速度为

v_i^{\mathrm{clo}}
=
\left[
-
\frac{l_i^\top v_i}{\|l_i\|_2+\varepsilon_l}
\right]_+,
\tag{8}
其中

[a]_+=\max(a,0).
据此定义近似碰撞时间

TTC_i
=
\frac{[\|l_i\|_2-R_i]_+}
{v_i^{\mathrm{clo}}+\varepsilon_v}.
\tag{9}
归一化 TTC 风险特征为

TTC_{\mathrm{norm},i}
=
\operatorname{clip}
\left(
1-\frac{TTC_i}{TTC_{\max}},
0,
1
\right).
\tag{10}
当 TTC_i 较大时，短期碰撞风险较低，TTC_{\mathrm{norm},i} 接近 0；当 TTC_i 较小时，短期碰撞风险较高，TTC_{\mathrm{norm},i} 接近 1。需要注意的是，TTC_i 作为归一化上下文风险特征用于调制语义安全余量 \beta_{i,t}；同一闭合速度几何也用于后续计算非负前向时间 T_i 和 \tau_i，但二者作用位置不同。

局部密度用于刻画当前障碍物附近的人群拥挤程度。设局部邻域半径为 R_\rho，则第 i 个障碍物邻域内的障碍物数量为

N_i^\rho
=
\sum_{j\ne i}
\mathbf 1
\left(
\|p_j^o(t)-p_i^o(t)\|_2\le R_\rho
\right).
\tag{11}
局部密度定义为

\rho_i
=
\frac{N_i^\rho}{\pi R_\rho^2}.
\tag{12}
其归一化形式为

\rho_{\mathrm{norm},i}
=
\operatorname{clip}
\left(
\frac{\rho_i}{\rho_{\max}},
0,
1
\right).
\tag{13}
综合上述上下文特征，定义第 i 个障碍物的上下文风险强度为

r_i
=
\operatorname{clip}
\left(
\lambda_h f_{\mathrm{head},i}
+
\lambda_T TTC_{\mathrm{norm},i}
+
\lambda_\rho \rho_{\mathrm{norm},i},
0,
1
\right),
\tag{14}
其中

\lambda_h\ge 0,\qquad
\lambda_T\ge 0,\qquad
\lambda_\rho\ge 0,
并满足

\lambda_h+\lambda_T+\lambda_\rho=1.
\tag{15}
r_i\in[0,1] 表示当前交互上下文下第 i 个障碍物的风险强度。风险越大，系统越倾向于启用更大的语义安全余量。

令 \mu_{\min}\in[0,1] 表示最低启用比例。上下文调制后的候选语义安全余量定义为

\hat\beta_{i,t}
=
\bar\beta_i
\left[
\mu_{\min}
+
(1-\mu_{\min})r_i
\right].
\tag{16}
结合类别映射，可写为

\hat\beta_{i,t}
=
\bar\Beta(c_{i,t})
\left[
\mu_{\min}
+
(1-\mu_{\min})r_i
\right].
\tag{17}
因此有

\mu_{\min}\bar\beta_i
\le
\hat\beta_{i,t}
\le
\bar\beta_i.
\tag{18}
这表示：类别 c_{i,t} 决定语义安全余量上限 \bar\beta_i，上下文风险 r_i 决定当前时刻实际倾向启用的比例。

为了避免语义安全余量在相邻采样时刻突然增大，进一步限制候选语义余量的正向变化速度。令 \bar\Delta\beta_i\ge0 表示允许的最大正向增量，则将候选余量修正为

\hat\beta_{i,t}
=
\min
\left\{
\bar\beta_i
\left[
\mu_{\min}
+
(1-\mu_{\min})r_i
\right],
\,
\beta_{i,t-1}+\bar\Delta\beta_i
\right\}.
\tag{19}
该式只限制语义安全余量的突然增大，不限制其降低。由此可保证

[\hat\beta_{i,t}-\beta_{i,t-1}]_+
\le
\bar\Delta\beta_i.
\tag{20}
最终的 \beta_{i,t} 还需要经过后续闭环分析中的带 Guard 裕度的可接受语义安全集 \mathcal B_i^\eta(X_t) 与 MPC 可行性守卫。换言之，\hat\beta_{i,t} 是由类别和上下文风险生成的候选语义安全余量，\beta_{i,t} 是最终被系统接受并实际进入 SEESM 和 MPC-SECBF 约束的语义安全余量。

在此基础上，SEESM 所覆盖的安全区域定义为

SR_{SEE}
=
\left\{
p(t)
\mid
\|l_i+\tau_i v_i\|_2>R_i+\beta_{i,t},\ \forall i
\right\}.
\tag{21}
其中，\tau_i 表示前向积分时间，\|l_i+\tau_i v_i\|_2 表示考虑相对速度后的拓展欧几里得距离。对于每一个障碍物，\tau_i 由动态风险门控因子确定：

\tau_i=f_r f_v f_T K_e T_i,
\tag{22a}
T_i=
\frac{[\|l_i\|_2-R_i]_+}
{v_i^{\mathrm{clo}}+\varepsilon_v},
\tag{22b}
f_r=\operatorname{sign}(v_i^{\mathrm{clo}}),
\tag{22c}
f_v=
\operatorname{sign}
\left(
\|v_i\|_2
\right),
\tag{22d}
f_T=\operatorname{sign}(T_{\max}-T_i).
\tag{22e}
符号函数定义为

\operatorname{sign}(x)
=
\begin{cases}
1, & x>0,\\
0, & x\le 0.
\end{cases}
\tag{23}
其中，K_e\in(0,1) 是松弛因子，用于调整 T_i 对 \tau_i 的影响；\delta_i 表示 l_i 与 v_i 之间的夹角。相对接近因子 f_r、相对速度因子 f_v 和时间视野因子 f_T 均为指示变量，用于判断当前动态交互是否需要启用拓展欧几里得距离项。由于 T_i 基于闭合速度定义，\tau_i 始终为非负前向时间。

需要强调的是，\tau_i 和 r_i 作用位置不同：\tau_i 作用于动态几何距离 \|l_i+\tau_i v_i\|_2，用于刻画速度导致的未来接近风险；r_i 作用于语义安全余量 \beta_{i,t}，用于根据语义类别、迎面接近程度、TTC 和局部密度动态调节额外安全距离。因此，二者不是重复项，而是分别调节 SEESM 的几何风险部分和语义风险部分。

因此，当满足以下条件时，SEESM 判定发生碰撞或违反安全边界：

F_i^{SEE}
=
\|l_i+\tau_i v_i\|_2
-
(R_i+\beta_{i,t})
\le 0.
\tag{24}
上述 SEESM 从动态几何风险和上下文语义风险两个层面给出了统一安全判据。为了将其纳入可优化控制框架，下一节进一步将该度量改写为 barrier 约束形式。

D. 基于 SEESM 的路径搜索算法

如算法 1 所示，受相关采样式规划方法启发，所提出的方法在 Expand(\cdot) 中对控制输入和持续时间进行采样，然后进行前向积分以获得下一时刻的候选运动基元。轨迹的实际代价 f_e 使用控制输入 u(t) 和轨迹持续时间 T 计算为

f_e(t)
=
\int_0^T\|u(t)\|_2^2\,dt+\rho T,
\tag{25}
其中，\rho 是用于惩罚较长持续时间的权重因子。启发式代价 f_h 通过求解一个两点边值问题获得，用以估计从当前状态到目标状态的最短距离代价。

在路径搜索过程中，本文基于 SEESM 对动态障碍物进行碰撞检查。在 CheckEESM(\cdot) 中，根据式 (21) 计算候选运动基元节点 node_m 与对应时间戳下每个预测障碍物之间的语义拓展欧几里得距离。如果任意预测障碍物违反式 (24) 的安全条件，则该运动基元节点被视为不安全。

因此，碰撞判定准则定义为

C_{ij}:
\|l_{ij}+\tau_{ij}v_{ij}\|_2
-
(R_i+\beta_{i,t})
\le 0.
\tag{26}
其中，l_{ij} 和 v_{ij} 分别表示基元节点与第 i 个预测障碍物在第 j 个预测时间戳下的相对位置和相对速度，\tau_{ij} 为对应的前向积分时间。这里的 \beta_{i,t} 与 C 节保持一致，由类别基准余量、上下文风险调制以及后续可行性守卫共同决定。路径搜索层不需要改变原有碰撞检查结构，只需将固定类别余量替换为最终接受的 \beta_{i,t}。

E. 语义扩展控制屏障函数

为了将 CBF 方法应用到存在移动障碍物的动态环境中，本文提出 SECBF 方法。该方法通过引入松弛变量来平衡安全性与可行性，同时利用 SEESM 构建 barrier 条件，以适应动态障碍物环境，并显式引入语义安全余量。

系统安全集定义为

Z=
\{X\in D\mid h(X)\ge0\},
其中 h:D\rightarrow\mathbb R 是连续可微函数。对于输入仿射系统，若存在扩展类 \mathcal K_\infty 函数 \gamma，则 CBF 条件可表示为

\sup_{u\in U}
\left[
\dot h(X,u)
\right]
\ge
-\gamma(h(X)),
\qquad
\gamma\in\mathcal K_\infty.
\tag{27a}
考虑移动障碍物位置变化对安全函数的影响，有

\dot h(X,u)
=
L_fh(X)+L_gh(X)u
+
\frac{\partial h}{\partial p^o}
\frac{\partial p^o}{\partial t}.
\tag{27b}
其中，u 表示受物理和运行约束限制的可行控制输入，L_fh(X) 和 L_gh(X) 分别表示 h(X) 沿系统漂移项和控制项方向的李导数。与只考虑静态障碍物的标准 CBF 相比，式 (27b) 中的 \frac{\partial h}{\partial p^o}\frac{\partial p^o}{\partial t} 显式体现了障碍物运动对安全函数变化率的影响。

为了将 CBF 约束纳入离散时间控制系统，采用离散时间形式：

h(X_{k+1})
\ge
(1-\gamma)h(X_k)-\epsilon_k,
\qquad
0<\gamma<1,
\qquad
0\le\epsilon_k\le\epsilon_{\max}.
\tag{28}
其中，\gamma 为标量参数，\epsilon_k 为松弛变量，用于在有限范围内放宽安全约束，以提升优化问题可行性。

从初始状态 k=0 开始，可以递推得到

h(X_k)
\ge
(1-\gamma)^k h(X_0)
-
\sum_{i=0}^{k-1}
(1-\gamma)^{k-1-i}\epsilon_i.
\tag{29}
若满足 \epsilon_i\le\epsilon_{\max}，则有

\sum_{i=0}^{k-1}
(1-\gamma)^{k-1-i}\epsilon_i
\le
\epsilon_{\max}
\sum_{j=0}^{k-1}(1-\gamma)^j.
\tag{30a}
又因为

\sum_{j=0}^{k-1}(1-\gamma)^j
=
\frac{1-(1-\gamma)^k}{\gamma},
\tag{30b}
代入式 (29) 可得

h(X_k)
\ge
(1-\gamma)^k h(X_0)
-
\epsilon_{\max}
\frac{1-(1-\gamma)^k}{\gamma}.
\tag{31}
若初始状态满足 X_0\in Z，即 h(X_0)\ge0，则有

h(X_k)
\ge
-
\epsilon_{\max}
\frac{1-(1-\gamma)^k}{\gamma}
\triangleq
-\kappa_k.
\tag{32}
当 k\rightarrow\infty 时，有

h(X_k)\ge-\frac{\epsilon_{\max}}{\gamma}.
\tag{33}
因此，离散时间松弛 CBF 可以保证系统具有一个稳定的 practical safety 下界。为使实际安全性满足 h(X)\ge-d_{\mathrm{tol}}，参数应满足

\epsilon_{\max}\le\gamma d_{\mathrm{tol}}.
接下来定义用于 MPC-SECBF 的具体安全函数。基于 CESM 的安全函数为

h_{CE}(X_k)
=
\|l_i(k)\|_2
-
R_i,
\qquad
k=0,1,\dots,N-1.
\tag{34a}
基于 EESM 的动态几何安全函数为

h_{EE}(X_k)
=
\|l_i(k)+\tau_i v_i(k)\|_2
-
R_i,
\qquad
k=0,1,\dots,N-1.
\tag{34b}
加入上下文风险调制语义余量后的 SEESM 安全函数为

h_{SEE}(X_k;\beta_{i,t})
=
\|l_i(k)+\tau_i v_i(k)\|_2
-
R_i
-
\beta_{i,t},
\qquad
k=0,1,\dots,N-1.
\tag{34c}
其中 R_i=R_r+R_{o,i}，因此式 (34c) 与代码中的核心形式一致：

h_{SEE}
=
\|p_{\mathrm{rel}}+\tau v_{\mathrm{rel}}\|_2
-
R_{\mathrm{obs}}
-
R_{\mathrm{robot}}
-
\beta_i.

\beta_{i,t} 由当前时刻 t 的语义类别 c_{i,t}、上下文风险 r_i、带安全裕度的可接受语义安全集 \mathcal B_i^\eta(X_t) 和 MPC 可行性共同决定。在单次 MPC 求解时域内，\beta_{i,t} 视为冻结参数，因此 h_{SEE} 仍满足与式 (28) 相同的松弛离散 CBF 递推关系。语义余量在跨采样时刻的更新机制和闭环安全性影响将在 H 节进一步讨论。

F. 侧向通过偏好代价

为了使机器人在靠近动态障碍物时具有一致的侧向通过偏好，引入固定通行规则

s_0\in\{-1,+1\},
\qquad
\text{本文取 }s_0=+1.
\tag{35}
定义二维旋转矩阵

J=
\begin{bmatrix}
0&-1\\
1&0
\end{bmatrix}.
\tag{36}
归一化相对方向向量定义为

\tilde n_{i,k}
:=
\frac{l_i(k)}
{\sqrt{\|l_i(k)\|_2^2+\varepsilon_n^2}}.
\tag{37}
对应的侧向方向定义为

\tilde t_{i,k}
:=
s_0J\tilde n_{i,k}.
\tag{38}
令

z_{i,k}^{EE}
=
l_i(k)+\tau_i v_i(k).
\tag{39}
侧向偏好量定义为

g_{i,k}^{\mathrm{side}}
:=
\tilde t_{i,k}^\top z_{i,k}^{EE}.
\tag{40}
单边惩罚函数定义为

\phi(r):=\max\{0,r\}^2.
\tag{41}
由此，侧向通过偏好代价写为

J_t^{\mathrm{side}}
=
\sum_{i=1}^{n}
\sum_{k=0}^{N_s-1}
w_{i,t}^{\mathrm{side}}
\phi
\left(
-g_{i,k}^{\mathrm{side}}
\right).
\tag{42}
其中，w_{i,t}^{\mathrm{side}} 表示侧向偏好权重。为保持主框架简洁，本文主要使用上下文风险 r_i 调制语义安全余量 \beta_{i,t}。如果希望进一步增强语义对象的侧向通过偏好，也可以令

w_{i,t}^{\mathrm{side}}
=
\bar w_i^{\mathrm{side}}
\left[
\mu_w+(1-\mu_w)r_i
\right],
\tag{43}
其中 \bar w_i^{\mathrm{side}} 为与障碍物类别相关的基准侧向权重，\mu_w\in[0,1] 为最低启用比例。若不启用该调制，则 w_{i,t}^{\mathrm{side}} 保持为原 version7 中的固定或经验设置。

G. Model Predictive Control

轨迹跟踪问题被构建为一个 MPC 优化任务，其目标是在确保安全性和可行性的前提下，将跟踪误差降至最低。机器人状态定义为

x_t=
[p_x(t),p_y(t),\theta(t)]^\top,
控制输入定义为

u_t=
[v(t),\omega(t)]^\top.
机器人离散时间运动学模型为

x_{t+1}
=
x_t
+
\begin{bmatrix}
\cos\theta(t)&0\\
\sin\theta(t)&0\\
0&1
\end{bmatrix}
u_t\Delta t.
\tag{44}
在预测时域 N 内，MPC 决策变量为

U=
[u_{t|t},u_{t+1|t},\dots,u_{t+N-1|t}],
松弛变量集合为

\Psi=
[\epsilon_0,\epsilon_1,\dots,\epsilon_{N-1}].
语义增强后的 MPC-SECBF 优化问题写为

\mathcal P_t^{\mathrm{sem}}:
\qquad
\min_{U,\Psi}
\left(
p(x_{t+N|t})
+
\sum_{k=0}^{N-1}
q(x_{t+k|t},u_{t+k|t})
+
\sum_{k=0}^{N-1}
\psi(\epsilon_k)
+
J_t^{\mathrm{side}}
\right).
\tag{45a}
其中

J_t^{\mathrm{side}}
=
\sum_{i=1}^{n}
\sum_{k=0}^{N_s-1}
w_{i,t}^{\mathrm{side}}
\phi
\left(
-g_{i,k}^{\mathrm{side}}
\right).
\tag{45b}
约束条件为

x_{t+k+1|t}
=
f(x_{t+k|t},u_{t+k|t}),
\qquad
k=0,\dots,N-1,
\tag{46a}
x_{t+k|t}\in X_{\mathrm{tra}}^k,
\qquad
k=0,\dots,N-1,
\tag{46b}
u_{t+k|t}\in U,
\qquad
k=0,\dots,N-1,
\tag{46c}
x_{t|t}=x_t,
\tag{46d}
x_{t+N|t}\in X_f.
\tag{46e}
短期预测域内采用更严格的语义安全约束：

h_{SEE}(X_{t+k+1|t};\beta_{i,t})
\ge
(1-\gamma)
h_{SEE}(X_{t+k|t};\beta_{i,t})
-
\epsilon_k,
\tag{46f}
\forall i=1,\dots,n,
\qquad
k=0,1,\dots,K_cN-1.
长期预测域内采用较宽松的物理安全约束：

h_{CE}(X_{t+k+1|t})
\ge
(1-\gamma)
h_{CE}(X_{t+k|t})
-
\epsilon_k,
\tag{46g}
k=K_cN,\dots,N-1.
目标函数中的终端代价、阶段代价和松弛变量惩罚分别定义为

p(x_{t+N|t})
=
\|x_{t+N|t}-x^d_{t+N|t}\|_P,
\tag{47a}
q(x_{t+k|t},u_{t+k|t})
=
\|x_{t+k|t}-x^d_{t+k|t}\|_Q
+
\|u_{t+k|t}\|_R
+
\|u_{t+k+1|t}-u_{t+k|t}\|_S,
\tag{47b}
\psi(\epsilon_k)
=
\lambda\epsilon_k^2.
\tag{47c}
其中，P,Q,R,S 为正定权重矩阵，\lambda 为松弛变量惩罚权重。目标函数由终端跟踪误差、阶段跟踪误差、控制输入代价、输入平滑项、松弛变量惩罚和侧向通过偏好代价共同组成。

辅助参数 K_c 用于限制 SECBF 条件在 MPC 预测时域内的有效范围。在避障过程中，短期安全性直接关系到实时避碰和系统稳定性，因此短期预测域采用包含语义余量 \beta_{i,t} 的 SECBF 约束。长期预测域中，由于机器人自身状态和障碍物预测状态的不确定性更大，可以使用相对宽松的 h_{CE} 约束以改善优化可行性。

H. 闭环安全理论分析

在前文分析中，单次 MPC 求解期间语义安全余量 \beta_{i,t} 被视为固定参数。但在闭环执行过程中，\beta_{i,t} 会随语义类别识别结果和上下文风险 r_i 在不同采样时刻更新，这相当于改变了安全函数阈值。因此，需要进一步分析语义更新对闭环安全性的影响。

H.1 语义安全集

语义增强不能凭空制造安全性，它只能消耗当前已有的物理安全余量。令 \eta\ge0 表示 Safety Guard 保留的最小物理裕度。对任意障碍物 i，定义当前状态 X_t 下带 Guard 裕度的可接受语义 margin 上界为

b_i^\eta(X_t)
=
\min
\left\{
\bar\beta_i,
\max\left(0,h_{EE}(X_t)-\eta\right)
\right\}.
\tag{48}

相应的可接受语义 margin 集为

\mathcal B_i^\eta(X_t)
=
\left[
0,
b_i^\eta(X_t)
\right].
\tag{49}
由语义物理安全函数定义

h_{SEE}(X_t;\beta)
=
h_{EE}(X_t)-\beta,
\tag{50}
可知

h_{SEE}(X_t;\beta)\ge\eta
\iff
h_{EE}(X_t)-\beta\ge\eta
\iff
\beta\le h_{EE}(X_t)-\eta.
\tag{51}
因此，当 h_{EE}(X_t)\ge\eta 时，\mathcal B_i^\eta(X_t) 中的任意 \beta 都满足 h_{SEE}(X_t;\beta)\ge\eta；当 h_{EE}(X_t)<\eta 时，系统不再施加额外语义收紧，并令 b_i^\eta(X_t)=0。

\mathcal B_i^\eta(X_t)\ne\varnothing.
\tag{52}
该引理说明，语义安全余量的可选范围受当前物理安全余量直接约束。只有当当前状态相对于基底物理安全边界仍保留足够安全余量时，系统才允许施加额外语义收紧；否则系统应退回较小的语义余量，最保守情况下退回 \beta=0。

H.2 上下文风险调制下的语义更新条件

由 C 节可知，类别和上下文风险共同生成候选语义安全余量

\hat\beta_{i,t}
=
\min
\left\{
\bar\beta_i
\left[
\mu_{\min}
+
(1-\mu_{\min})r_i
\right],
\,
\beta_{i,t-1}+\bar\Delta\beta_i
\right\}.
\tag{53}
其中

r_i
=
\operatorname{clip}
\left(
\lambda_h f_{\mathrm{head},i}
+
\lambda_T TTC_{\mathrm{norm},i}
+
\lambda_\rho \rho_{\mathrm{norm},i},
0,
1
\right).
\tag{54}
为避免语义在相邻采样时刻过度收紧，从而破坏当前安全性或优化可行性，本文采用如下守卫式更新机制：

\beta_{i,t}^{\mathrm{proj}}
=
\Pi_{\mathcal B_i^\eta(X_t)}
\left(
\hat\beta_{i,t}
\right),
\qquad
\beta_{i,t}^{\mathrm{fb}}
=
\Pi_{\mathcal B_i^\eta(X_t)}
\left(
\beta_{i,t-1}
\right),
\tag{55}

\beta_{i,t}
=
\begin{cases}
\beta_{i,t}^{\mathrm{proj}},
&
\mathcal P_t^{\mathrm{sem}}
\left(
\beta_{i,t}^{\mathrm{proj}}
\right)
\ \text{可行},
\\[1mm]
\beta_{i,t}^{\mathrm{fb}},
&
\mathcal P_t^{\mathrm{sem}}
\left(
\beta_{i,t}^{\mathrm{fb}}
\right)
\ \text{可行},
\\[1mm]
0,
&
\text{否则}.
\end{cases}
\tag{56}
其中，\Pi_{\mathcal B_i^\eta(X_t)}(\cdot) 表示投影到当前可接受语义 margin 集，\mathcal P_t^{\mathrm{sem}}(\beta) 表示使用给定语义余量 \beta 的语义增强 MPC 优化问题。式 (55)--(56) 表示，候选语义安全余量先被投影到当前 Guard 上界；若对应的 MPC 优化问题不可行，则投影上一时刻的语义余量作为回退；若仍不可行，则退回 \beta=0，避免在临界状态下继续施加额外语义收紧。

由于式 (53) 已经限制了候选语义安全余量的正向增量，且式 (55)--(56) 的投影和回退不会产生比候选余量更大的正向增量，因此可得

[\beta_{i,t}-\beta_{i,t-1}]_+
\le
\bar\Delta\beta_i.
\tag{57}
这说明，语义 margin 不再是任意跳变的外部参数，而是受到类别基准余量、上下文风险、当前物理安全余量和 MPC 可行性共同约束。

H.3 闭环 practical safety

定义闭环安全量

H_{i,t}
:=
h_{SEE}(X_t;\beta_{i,t}).
\tag{58}
在时刻 t 的 MPC 求解中，\beta_{i,t} 在预测域内冻结，因此第一拍满足

h_{SEE}(X_{t+1};\beta_{i,t})
\ge
(1-\gamma)
h_{SEE}(X_t;\beta_{i,t})
-
\epsilon_{i,t}.
\tag{59}
由 H_{i,t} 的定义，有

h_{SEE}(X_t;\beta_{i,t})=H_{i,t}.
下一时刻的闭环安全量为

H_{i,t+1}
=
h_{SEE}(X_{t+1};\beta_{i,t+1}).
\tag{60}
又因为

h_{SEE}(X_{t+1};\beta_{i,t+1})
=
h_{SEE}(X_{t+1};\beta_{i,t})
-
(\beta_{i,t+1}-\beta_{i,t}),
\tag{61}
定义正向语义增量

\Delta\beta_i(t)
:=
[\beta_{i,t+1}-\beta_{i,t}]_+.
\tag{62}
结合式 (59)--(62)，可得

H_{i,t+1}
\ge
(1-\gamma)H_{i,t}
-
\epsilon_{i,t}
-
\Delta\beta_i(t).
\tag{63}
递推展开得

H_{i,t}
\ge
(1-\gamma)^tH_{i,0}
-
\sum_{j=0}^{t-1}
(1-\gamma)^{t-1-j}
\left(
\epsilon_{i,j}
+
\Delta\beta_i(j)
\right).
\tag{64}
若满足

0\le\epsilon_{i,t}\le\epsilon_{\max},
\qquad
0\le\Delta\beta_i(t)\le\bar\Delta\beta_i,
\tag{65}
则有

H_{i,t}
\ge
(1-\gamma)^tH_{i,0}
-
(\epsilon_{\max}+\bar\Delta\beta_i)
\sum_{j=0}^{t-1}
(1-\gamma)^j.
\tag{66}
由几何级数公式

\sum_{j=0}^{t-1}(1-\gamma)^j
=
\frac{1-(1-\gamma)^t}{\gamma},
\tag{67}
可得

H_{i,t}
\ge
(1-\gamma)^tH_{i,0}
-
(\epsilon_{\max}+\bar\Delta\beta_i)
\frac{1-(1-\gamma)^t}{\gamma}.
\tag{68}
若初始状态满足

H_{i,0}\ge0,
则有闭环 practical safety 下界

\inf_{t\ge0}H_{i,t}
\ge
-
\frac{\epsilon_{\max}+\bar\Delta\beta_i}{\gamma}.
\tag{69}
即

\inf_{t\ge0}
h_{SEE}(X_t;\beta_{i,t})
\ge
-
\frac{\epsilon_{\max}+\bar\Delta\beta_i}{\gamma}.
\tag{70}
该结果表明，在松弛变量有界、语义安全余量正向增量有界的条件下，上下文风险调制不会破坏闭环 practical safety；它只会将原本由 \epsilon_{\max} 决定的安全下界扩展为由 \epsilon_{\max}+\bar\Delta\beta_i 共同决定。

当不启用语义安全余量时，有

\beta_{i,t}\equiv0.
\tag{71}
此时

h_{SEE}(X_t;0)=h_{EE}(X_t).
\tag{72}
若同时不启用侧向通过偏好代价，即

w_{i,t}^{\mathrm{side}}\equiv0,
\tag{73}
则所提语义增强框架退化为不含语义项的基准分层 MPC-ECBF 结构。相应地，闭环 practical safety 下界退化为

\inf_{t\ge0}
h_{EE}(X_t)
\ge
-
\frac{\epsilon_{\max}}{\gamma}.
\tag{74}
核心公式汇总

类别基准语义安全余量：

\bar\beta_i=\bar\Beta(c_{i,t}).
上下文风险调制：

r_i
=
\operatorname{clip}
\left(
\lambda_h f_{\mathrm{head},i}
+
\lambda_T TTC_{\mathrm{norm},i}
+
\lambda_\rho \rho_{\mathrm{norm},i},
0,
1
\right).
候选语义安全余量：

\hat\beta_{i,t}
=
\min
\left\{
\bar\beta_i
\left[
\mu_{\min}
+
(1-\mu_{\min})r_i
\right],
\,
\beta_{i,t-1}+\bar\Delta\beta_i
\right\}.
带 Guard 裕度的可接受语义安全集：

b_i^\eta(X_t)
=
\min
\left\{
\bar\beta_i,
\max\left(0,h_{EE}(X_t)-\eta\right)
\right\},
\qquad
\mathcal B_i^\eta(X_t)
=
\left[0,b_i^\eta(X_t)\right].
最终语义更新律：

\beta_{i,t}^{\mathrm{proj}}
=
\Pi_{\mathcal B_i^\eta(X_t)}
\left(
\hat\beta_{i,t}
\right),
\qquad
\beta_{i,t}^{\mathrm{fb}}
=
\Pi_{\mathcal B_i^\eta(X_t)}
\left(
\beta_{i,t-1}
\right),

\beta_{i,t}
=
\begin{cases}
\beta_{i,t}^{\mathrm{proj}},
&
\mathcal P_t^{\mathrm{sem}}
\left(
\beta_{i,t}^{\mathrm{proj}}
\right)
\ \text{可行},
\\[1mm]
\beta_{i,t}^{\mathrm{fb}},
&
\mathcal P_t^{\mathrm{sem}}
\left(
\beta_{i,t}^{\mathrm{fb}}
\right)
\ \text{可行},
\\[1mm]
0,
&
\text{否则}.
\end{cases}
SEESM 安全函数：

h_{SEE}(X_k;\beta_{i,t})
=
\|l_i(k)+\tau_i v_i(k)\|_2
-
R_i
-
\beta_{i,t}.
其中 R_i=R_r+R_{o,i}，等价于代码中的

h_{SEE}
=
\|p_{\mathrm{rel}}+\tau v_{\mathrm{rel}}\|_2
-
R_{\mathrm{obs}}
-
R_{\mathrm{robot}}
-
\beta_i.
MPC-SECBF 约束：

h_{SEE}(X_{t+k+1|t};\beta_{i,t})
\ge
(1-\gamma)
h_{SEE}(X_{t+k|t};\beta_{i,t})
-
\epsilon_k.
闭环 practical safety 下界：

\inf_{t\ge0}
h_{SEE}(X_t;\beta_{i,t})
\ge
-
\frac{\epsilon_{\max}+\bar\Delta\beta_i}{\gamma}.
这一版的逻辑关系是：

c_{i,t}
\rightarrow
\bar\beta_i
\rightarrow
r_i
\rightarrow
\hat\beta_{i,t}
\rightarrow
\mathcal B_i^\eta(X_t)
\rightarrow
\beta_{i,t}
\rightarrow
h_{SEE}
\rightarrow
\mathcal P_t^{\mathrm{sem}}
\rightarrow
\text{closed-loop practical safety}.
