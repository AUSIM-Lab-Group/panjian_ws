# 2026-06-15 实车调试必看

## 当前结论

今天已经把实车链路推进到 dry-run 阶段：FAST-LIO、相机、YOLO、语义融合、动态感知、全局前端和 MPC-SECBF 都能启动，`/cmd_vel_secbf_dryrun` 可以输出控制量。当前不要接真实 `/cmd_vel`，仍然只做 dry-run。

最重要的调试结论：

1. `0.5m` 目标不适合用来判断全局前端是否正常。
   - `ThetaAstar` 里 `near_end` 容差约等于 `1.0m`。
   - 发 `x=0.5` 时，规划器可能一开始就认为已经接近终点，但起点节点没有 parent，直接输出 `no path`。
   - 明天测试目标先用 `x=2.0` 或 `x=3.0`。

2. 天线不是当前主要根因。
   - 拔掉雷达后方两根天线后问题仍存在。
   - 实时统计过：机器人周围 `0.8m` 内 `/sdf_map/occupancy_inflate` 和 `/fastLIO/non_ground_points` 都没有占据点；正前方 `0.6m` 走廊也没有占据点。
   - 静态地图/自车点云不是这次 `no path` 的主要原因。

3. 终端6 使用 `front_adsm:=false` 后，全局前端可以规划。
   - 终端6 应出现：`------ path searching use None ------`
   - 这说明 `front_adsm` 已关闭，前端不再用 ADSM 动态障碍检查。
   - 如果 `x=2.0` 或 `x=3.0` 能出 `/global_path`，说明静态地图和基本前端是通的。

4. 当前主要问题变成动态预测不稳定。
   - RViz 里飞来飞去的蓝色圆形障碍物来自 `/obstacle_prediction_node/obstacle_prediction/trajs_predicted` 和 `/globalFsm_by_adsm/obs_predict_pub`。
   - 抓到过异常预测，例如：
     - `v=(5.33,-2.76), a=(22.42,-12.08)`
     - `v=(6.80,-4.08), a=(32.25,-19.52)`
   - 这些假障碍/飞行轨迹进入 MPC-SECBF 后，会导致 CBF 约束偶发不可行。

5. 终端6 偶发输出 `Infeasible_Problem_Detected` 是动态预测抖动导致的。
   - 看到 `[MPC-SECBF] Fallback (no CBF) succeeded` 表示 fallback 成功，系统没有崩。
   - 但这时不能上真实车，因为 CBF 主优化不是稳定可行。

## 明天优先做什么

1. 先按下面 8 个终端命令恢复今天的 dry-run 链路。
2. 终端6 先保持 `front_adsm:=false`，验证全局路径能稳定出来。
3. 目标先发 `x=2.0`，不要发 `x=0.5`。
4. 如果全局路径稳定，下一步修动态预测清洗：
   - 过滤 `|v| > 2.0 m/s` 的障碍。
   - 过滤 `|a| > 3.0 m/s^2` 的障碍。
   - 过滤 `radius > 0.8m` 的障碍。
   - 过滤 z 高度明显异常或跳变大的预测。
   - 障碍连续出现 N 帧后再进入 `/globalFsm_by_adsm/obs_predict_pub` 和 MPC-SECBF。
5. 修好预测过滤后，再尝试打开 `front_adsm:=true`。
6. 在 `front_adsm:=true`、MPC-SECBF 无频繁 infeasible、急停/底盘都确认前，不要切真实 `/cmd_vel`。

## 8 个终端命令

### 终端1：ROS master

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
roscore
```

### 终端2：LiDAR + IMU + FAST-LIO + RealSense

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
roslaunch all_demo all_demo.launch
```

### 终端3：临时相机外参 TF

当前只是临时外参，先用于跑通链路。真实实验前要标定替换。

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
rosrun tf2_ros static_transform_publisher 0.2 0.0 -0.3 0 0 0 body camera_link
```

### 终端4：几何动态感知

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
roslaunch swarm_test start_perception.launch use_sim_time:=false show_rviz:=false
```

### 终端5：YOLO 语义检测

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
roslaunch semantic_detection yolo.launch image_topic:=/camera/color/image_raw device:=cuda:0 confidence:=0.5
```

### 终端6：MPC-SECBF dry-run 规划控制

注意：当前先关闭前端 ADSM，只验证全局路径和 MPC-SECBF dry-run。

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
roslaunch swarm_test exp_secbf_planner.launch \
  cmd_vel_topic:=/cmd_vel_secbf_dryrun \
  v_max:=0.3 \
  front_adsm:=false
```

终端6 启动后应看到：

```text
------ path searching use None ------
```

### 终端7：RViz

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash
rviz -d /home/lxr20/lxr/panjian_ws/swarm_test/config/secbf_real.rviz
```

RViz 重点看：

- `/global_path`
- `/vis_kino_traj`
- `/sdf_map/occupancy_inflate`
- `/fastLIO/non_ground_points`
- `/globalFsm_by_adsm/obs_traj_vis`
- `/cmd_vel_secbf_dryrun` 可用 `rostopic echo` 看

如果 RViz 报 `/odom_traj` 类型不匹配，只是显示配置问题，可以先关掉对应 Display。

### 终端8：发目标点和诊断命令

先发 `x=2.0`，不要先发 `x=0.5`。

```bash
source /opt/ros/noetic/setup.bash
source /home/lxr20/lxr/panjian_ws/devel/setup.bash

rostopic pub -1 /move_base_simple/goal geometry_msgs/PoseStamped "header:
  stamp: {secs: 0, nsecs: 0}
  frame_id: 'camera_init'
pose:
  position: {x: 2.0, y: 0.0, z: 0.0}
  orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}"
```

如果 `x=2.0` 成功，再试 `x=3.0`：

```bash
rostopic pub -1 /move_base_simple/goal geometry_msgs/PoseStamped "header:
  stamp: {secs: 0, nsecs: 0}
  frame_id: 'camera_init'
pose:
  position: {x: 3.0, y: 0.0, z: 0.0}
  orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}"
```

看 dry-run 控制输出：

```bash
rostopic echo /cmd_vel_secbf_dryrun
```

看动态预测是否还飞：

```bash
rostopic echo -n1 /obstacle_prediction_node/obstacle_prediction/trajs_predicted
rostopic echo -n1 /globalFsm_by_adsm/obs_predict_pub
```

## 明天要修的代码方向

### 方向1：修全局前端起始速度

文件：

- `planner/vomp_planner/traj_planner/example/globalFsm_by_adsm.cpp`

问题：

- 当前代码里有 `double v_max = 1.3`。
- 初始规划时用 `start_vel = v_max * 0.8`，即约 `1.04m/s`。
- 重规划时还有 `init_vel = 1.4` 的硬编码。
- 实车静止时这样不合理，容易让动态检查或运动原语筛选过保守。

建议：

- 初始规划使用 `cur_vel_`，或者从 launch 参数读入低速前端速度。
- dry-run 阶段建议前端速度限制与 MPC `v_max:=0.3` 一致。

### 方向2：加动态预测过滤器

位置可选：

- 在 `obstacle_prediction_node` 输出前过滤。
- 或在 `globalFsm_by_adsm` 的 `Obs_Manager::predict_Traj_Callback` 中过滤。
- 或在进入 MPC-SECBF 前过滤 `/globalFsm_by_adsm/obs_predict_pub`。

建议先在 `Obs_Manager::predict_Traj_Callback` 中做最小过滤，便于同时保护前端和 MPC：

- `abs(vx), abs(vy)` 或 `sqrt(vx^2+vy^2)` 超阈值丢弃。
- `abs(ax), abs(ay)` 或 `sqrt(ax^2+ay^2)` 超阈值丢弃。
- 外接圆半径过大丢弃。
- z 高度/方差异常的预测丢弃。
- 同一障碍连续稳定出现若干帧后再写入 `obstalce_trajs_`。

## 当前修改记录

- 今天曾在 FAST-LIO 里加过 `/fastLIO/non_ground_points` 发布前的 `self_filter`，用于过滤车体/天线附近点云。
- 后续实验证明天线不是主要问题，但该过滤对实车仍有价值，可以保留。
- 该修改已编译通过：`[100%] Built target fastlio_mapping`。

## 安全提醒

当前仍然不要接真实 `/cmd_vel`。

只有满足下面条件后才考虑真实底盘：

1. `front_adsm:=false` 下全局路径和 MPC dry-run 稳定。
2. 动态预测不再出现飞行蓝色圆形障碍物。
3. MPC-SECBF 不再频繁 `Infeasible_Problem_Detected`。
4. 急停、底盘 CAN、低速限幅都确认。
5. 相机外参从临时 TF 替换为真实标定。
